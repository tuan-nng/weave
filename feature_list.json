{
  "$schema": "./feature_list.schema.json",
  "wip_limit": 1,
  "phases": [
    {
      "id": "phase-1",
      "name": "Core Foundation",
      "description": "Binary starts, database works, providers connect, sessions stream. The minimum viable agent platform.",
      "features": [
        "feat-001",
        "feat-002",
        "feat-003",
        "feat-004",
        "feat-005",
        "feat-006",
        "feat-007",
        "feat-008",
        "feat-009",
        "feat-010"
      ]
    },
    {
      "id": "phase-2",
      "name": "Agent Tools & Observability",
      "description": "Agents can execute tools server-side. Traces and journey view provide visibility into agent behavior.",
      "features": [
        "feat-011",
        "feat-012",
        "feat-013",
        "feat-014",
        "feat-015",
        "feat-016",
        "feat-017",
        "feat-018"
      ]
    },
    {
      "id": "phase-3",
      "name": "Frontend",
      "description": "Browser UI for creating sessions, chatting with agents, and observing their work.",
      "features": [
        "feat-019",
        "feat-020",
        "feat-021",
        "feat-022",
        "feat-023",
        "feat-036",
        "feat-061"
      ]
    },
    {
      "id": "phase-4",
      "name": "Kanban",
      "description": "Kanban boards with task management and lane automation that triggers specialist sessions.",
      "features": [
        "feat-024",
        "feat-025",
        "feat-026",
        "feat-027"
      ]
    },
    {
      "id": "phase-5",
      "name": "Extended Features",
      "description": "Agent-to-agent communication, notes, artifacts, codebase management, and production readiness.",
      "features": [
        "feat-028",
        "feat-029",
        "feat-030",
        "feat-031",
        "feat-032",
        "feat-033",
        "feat-034",
        "feat-035"
      ]
    },
    {
      "id": "phase-6",
      "name": "Native Tool Loop",
      "description": "Complete the native Anthropic tool-execution loop. Prerequisite for all wrapped-CLI work because journey and trace shapes depend on tools actually executing and being recorded. Anthropic API in native mode must keep its current behavior and gain a real tool-use loop.",
      "features": [
        "feat-037"
      ]
    },
    {
      "id": "phase-7",
      "name": "Multi-Runtime Foundation",
      "description": "Schema, model, and trait surface changes to carry runtime/mode on a session and a per-Runtime-Tool provider config. No user-facing surface change yet. The Provider table widens to a discriminated union (HTTP vs CLI); the CodingAgent trait gains a per-turn context; runtime x mode compatibility is enforced at session creation.",
      "features": [
        "feat-038",
        "feat-039",
        "feat-040",
        "feat-041",
        "feat-042"
      ]
    },
    {
      "id": "phase-8",
      "name": "Claude Code CLI Wrapped Mode",
      "description": "First CLI runtime target. Per-turn subprocess runner, fake-CLI test harness, Claude Code stream-json parser, PermissionMapper, CLI-native resume id persistence with message-history replay fallback, CLI -> journey translation, child-process reaping on crash, workspace-scoped CLI session validation, and an end-to-end CliCodingAgent conformance test. Anthropic API in native mode stays green throughout.",
      "features": [
        "feat-043",
        "feat-044",
        "feat-045",
        "feat-046",
        "feat-047",
        "feat-048",
        "feat-049",
        "feat-050",
        "feat-051"
      ]
    },
    {
      "id": "phase-9",
      "name": "Multi-Runtime User Surface",
      "description": "Settings, session creation, kanban, and A2A surfaces that let a user pick a Runtime Tool explicitly. The silent first-provider fallback is replaced with explicit Runtime Tool selection or a configured default.",
      "features": [
        "feat-052",
        "feat-053",
        "feat-054",
        "feat-055",
        "feat-056"
      ]
    },
    {
      "id": "phase-10",
      "name": "Additional CLI Runtimes",
      "description": "Codex and OpenCode adapters, built on the shared CliRunner, parser framework, PermissionMapper, fake-CLI harness, and conformance suite from Phase 8. Each adapter must pass the conformance suite to ship. Real CLI smoke tests are manual.",
      "features": [
        "feat-057",
        "feat-058",
        "feat-059"
      ]
    },
    {
      "id": "phase-11",
      "name": "Attended Mode (Deferred)",
      "description": "Attended mode is a separate Terminal abstraction (not a CodingAgent impl). Deferred until Phase 8 wrapped mode is stable and the lifecycle split (Weave drives the CLI vs. the user drives the CLI) is justified by real usage.",
      "features": [
        "feat-060"
      ]
    },
    {
      "id": "phase-12",
      "name": "Kanban × Claude Code Coordination",
      "description": "Turn the kanban from a fire-and-forget trigger into a coordination surface between the board and the Claude Code agent. Splits docs/design-docs/kanban-claude-code-coordination.md into 5 implementation tasks, each ≤1000 lines of changes: rich prompt with stage + gate + history context (feat-063), six new kanban tools callable by the agent (feat-064, may need a sub-split), per-column stage enum with stage-aware prompts and tool filtering (feat-065), full transition-gate enforcement with delivery/contract/checklist/validator rules and a gate_mode toggle (feat-066), and lifecycle supervision (watchdog, recovery re-prompt, failure broadcast) for kanban-auto-spawned sessions (feat-067).",
      "features": [
        "feat-063",
        "feat-064",
        "feat-065",
        "feat-066",
        "feat-067"
      ]
    }
  ],
  "features": [
    {
      "id": "feat-001",
      "phase": "phase-1",
      "behavior": "Binary compiles and starts. CLI accepts --host, --port, --db-path, --allow-remote flags via clap. Tracing subscriber initializes with RUST_LOG env filter. Server binds and listens. GET /api/health returns {\\\\\\\"status\\\\\\\": \\\\\\\"ok\\\\\\\", \\\\\\\"version\\\\\\\": \\\\\\\"...\\\\\\\", \\\\\\\"uptime_seconds\\\\\\\": N}. Graceful shutdown on SIGTERM/SIGINT.",
      "verification": "cargo build -p weave-server && cargo run -p weave-server -- --port 19876 &; sleep 2; curl -sf http://localhost:19876/api/health | jq -e '.status == \\\\\\\"ok\\\\\\\"'; kill %1",
      "state": "passing",
      "dependencies": [],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-002",
      "phase": "phase-1",
      "behavior": "SQLite opens with WAL mode, foreign keys ON, 5s busy timeout. Versioned migrations run on startup via include_str! embedded SQL files. Schema creates all 11 tables: workspaces, sessions, messages, providers, codebases, worktrees, boards, columns, tasks, traces, file_changes. Indexes on foreign keys and status columns.",
      "verification": "cargo test -p weave-server -- test_db_init && cargo test -p weave-server -- test_migrations_idempotent",
      "state": "passing",
      "dependencies": [
        "feat-001"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-003",
      "phase": "phase-1",
      "behavior": "Shared error types: AppError enum (NotFound, Validation, Provider, Database, Internal) with thiserror. ProviderError enum (AuthFailed, RateLimited, ModelNotFound, Unreachable, StreamInterrupted). AppError implements IntoResponse mapping errors to HTTP status codes + consistent JSON envelope {\\\\\\\"error\\\\\\\": {\\\\\\\"code\\\\\\\": \\\\\\\"...\\\\\\\", \\\\\\\"message\\\\\\\": \\\\\\\"...\\\\\\\"}}.",
      "verification": "cargo test -p weave-server -- test_error_response_format && cargo test -p weave-server -- test_error_status_codes",
      "state": "passing",
      "dependencies": [
        "feat-001"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-004",
      "phase": "phase-1",
      "behavior": "WorkspaceStore with CRUD operations. Default workspace \\\\\\\"default\\\\\\\" created on first startup. API: GET /api/workspaces (paginated), POST /api/workspaces (name validation 1-100 chars), GET /api/workspaces/:id, PATCH /api/workspaces/:id, DELETE /api/workspaces/:id (cascade deletes). All responses use consistent JSON envelope.",
      "verification": "cargo test -p weave-server -- test_workspace_crud && cargo test -p weave-server -- test_workspace_default_seed",
      "state": "passing",
      "dependencies": [
        "feat-002",
        "feat-003"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-005",
      "phase": "phase-1",
      "behavior": "CodingAgent trait defined: provider_type(), display_name(), list_models(), send_message(MessageRequest) -> Stream<StreamEvent>, health_check(). StreamEvent enum: TextDelta, ToolUseStart, ToolUseDelta, ToolResult, Thinking, Done(StopReason), Error. StopReason enum: EndTurn, MaxTokens, ToolUse, Cancelled. Supporting types: MessageRequest, Message, Role, Content, ContentBlock, ToolDefinition, ModelInfo, ProviderHealth. All types are Send + Sync.",
      "verification": "cargo test -p weave-server -- test_stream_event_variants && cargo build -p weave-server (trait compiles)",
      "state": "passing",
      "dependencies": [
        "feat-001"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-006",
      "phase": "phase-1",
      "behavior": "AnthropicAgent implements CodingAgent. Sends POST to {base_url}/v1/messages with stream:true. Parses Anthropic SSE events (message_start, content_block_start, content_block_delta, content_block_stop, message_delta, message_stop) into StreamEvent items. Handles errors: 401->AuthFailed, 429->RateLimited (with Retry-After), 404->ModelNotFound, 500/529->retryable. list_models() returns hardcoded Claude model list. health_check() sends minimal request to verify credentials.",
      "verification": "cargo test -p weave-server -- test_anthropic_sse_parsing && cargo test -p weave-server -- test_anthropic_error_mapping",
      "state": "passing",
      "dependencies": [
        "feat-005"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-007",
      "phase": "phase-1",
      "behavior": "ProviderStore persists providers to SQLite. ProviderRegistry holds HashMap<String, Arc<dyn CodingAgent>>, loads from DB on startup. Runtime CRUD: add_provider (validates config, creates agent, inserts DB), remove_provider (fails if active sessions reference it), get_agent by ID. API: GET /api/providers (strips api_key from response), POST /api/providers, DELETE /api/providers/:id, GET /api/providers/:id/models.",
      "verification": "cargo test -p weave-server -- test_provider_crud && cargo test -p weave-server -- test_provider_api_key_stripped",
      "state": "passing",
      "dependencies": [
        "feat-004",
        "feat-006"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-008",
      "phase": "phase-1",
      "behavior": "SessionStore persists sessions and messages. Session state machine: connecting -> ready -> completed/cancelled/error. API: POST /api/workspaces/:wid/sessions (create with provider_id, optional specialist_id/model/parent_session_id), GET /api/workspaces/:wid/sessions (paginated), GET /api/sessions/:id, DELETE /api/sessions/:id. Messages stored as immutable JSON content blocks. GET /api/sessions/:sid/history with cursor-based pagination.",
      "verification": "cargo test -p weave-server -- test_session_lifecycle && cargo test -p weave-server -- test_session_state_transitions && cargo test -p weave-server -- test_message_pagination",
      "state": "passing",
      "dependencies": [
        "feat-007"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-009",
      "phase": "phase-1",
      "behavior": "SessionService orchestrates session lifecycle. send_prompt: validates session status, saves user message, loads specialist system prompt (if set), builds message history, resolves tool set (initially empty), calls agent.send_message, spawns async task that streams events -> broadcasts to SSE channel -> accumulates response -> saves assistant message -> updates session status. POST /api/sessions/:sid/prompt returns message_id immediately. POST /api/sessions/:sid/cancel aborts the agent stream.",
      "verification": "cargo test -p weave-server -- test_send_prompt_flow && cargo test -p weave-server -- test_cancel_session",
      "state": "passing",
      "dependencies": [
        "feat-008"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-010",
      "phase": "phase-1",
      "behavior": "SSE infrastructure: SseManager with per-entity broadcast channels (tokio::broadcast). EventBuffer with ring buffer (100 events per entity). GET /api/sessions/:sid/stream returns SSE stream with connected, text_delta, tool_use_start, tool_use_delta, tool_result, thinking, done, error, heartbeat (15s) events. Supports Last-Event-ID header for reconnection. Backpressure: lagged subscriber receives gap event. SessionService broadcasts all agent events through this layer.",
      "verification": "cargo test -p weave-server -- test_broadcast_and_subscribe && cargo test -p weave-server -- test_event_buffer_replay && cargo test -p weave-server -- test_sse_broadcast_on_prompt",
      "state": "passing",
      "dependencies": [
        "feat-009"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-011",
      "phase": "phase-2",
      "behavior": "SpecialistLoader scans resources/specialists/*.md, parses YAML frontmatter (name, description, model, tool_profile, tags), extracts markdown body as system prompt. Loads into HashMap<String, Specialist> at startup. Logs count of loaded specialists and skipped files. Injects system prompt into session's first message when specialist_id is set. Handles: malformed YAML (skip + warn), missing required fields (skip + warn), invalid UTF-8 (skip + warn).",
      "verification": "cargo test -p weave-server -- test_specialist_loading && cargo test -p weave-server -- test_specialist_frontmatter_parse && cargo test -p weave-server -- test_specialist_malformed_yaml",
      "state": "passing",
      "dependencies": [
        "feat-009"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-012",
      "phase": "phase-2",
      "behavior": "ToolRegistry holds HashMap<String, Arc<dyn ToolExecutor>> and profiles HashMap<String, Vec<String>>. ToolExecutor trait: name(), input_schema(), execute(input, context) -> ToolContext contains session_id, cwd, codebase_root, trace_collector. ToolResult: {success, data, error}. Five profiles defined: full (all tools), implementation (fs+shell+git+task), review (fs-read+git+task+artifacts), planning (task+kanban+notes), reporting (task-read+notes+artifacts). SessionService filters tools by specialist's profile when building MessageRequest.",
      "verification": "cargo test -p weave-server -- test_tool_profile_filtering && cargo test -p weave-server -- test_send_prompt_invalid_tool_profile",
      "state": "passing",
      "dependencies": [
        "feat-005"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-013",
      "phase": "phase-2",
      "behavior": "Filesystem tools: fs_read (read file content), fs_write (write/overwrite, creates parent dirs), fs_edit (search/replace patch, unique match required), fs_search (regex search with glob filter), fs_list (directory listing, recursive option). All paths must be absolute. Write operations restricted to session's codebase root (symlink-aware canonicalization, no .. traversal). Control-plane paths (.git/, weave.db, Cargo.toml, resources/specialists/, resources/migrations/, .claude/, CLAUDE.md, DECISIONS.md, PROGRESS.md, feature_list.json, init.sh, justfile) are read-only. Tool results include success flag, bytes written/read, error messages with fix instructions.",
      "verification": "cargo test -p weave-server -- test_file_write_read && cargo test -p weave-server -- test_file_path_containment && cargo test -p weave-server -- test_control_plane_protection",
      "state": "passing",
      "dependencies": [
        "feat-012",
        "feat-011"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-014",
      "phase": "phase-2",
      "behavior": "Shell tool: shell_exec with command, optional cwd, optional timeout_ms (default 30000). Runs via tokio::process::Command. Captures stdout + stderr + exit code. Timeout kills the process. Cwd defaults to session's working directory. Output truncated at 100KB. Records trace event with duration.",
      "verification": "cargo test -p weave-server -- test_shell_exec_basic && cargo test -p weave-server -- test_shell_exec_timeout && cargo test -p weave-server -- test_shell_exec_cwd",
      "state": "passing",
      "dependencies": [
        "feat-012"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-015",
      "phase": "phase-2",
      "behavior": "Git tools: git_status (branch, staged/unstaged/untracked), git_diff (unified diff, truncated 50KB, optional staged/file filter), git_log (recent commits with hash+message, default 10), git_commit (message, optional stage_all, validates real user.name/user.email \\\\u2014 rejects placeholder/test identities). All tools take cwd parameter, default to session working directory.",
      "verification": "cargo test -p weave-server -- test_git_status && cargo test -p weave-server -- test_git_commit_validation",
      "state": "passing",
      "dependencies": [
        "feat-012"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-016",
      "phase": "phase-2",
      "behavior": "Task context tools: get_task (current task details with title, description, acceptance criteria), list_tasks (filter by status/column_id), update_task_status (in_progress, review_required, completed, needs_fix, blocked), update_task_fields (completion_summary, verification_report). These tools are the bridge between kanban tasks and agent sessions \\\\u2014 agents know what they're working on.",
      "verification": "cargo test -p weave-server -- test_get_task_context && cargo test -p weave-server -- test_update_task_status",
      "state": "passing",
      "dependencies": [
        "feat-012"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-017",
      "phase": "phase-2",
      "behavior": "TraceCollector records events during agent execution. Traces persisted to SQLite. Event types: tool_call (name, input, output, duration_ms), file_change (path, action: read/write/create/delete), decision (extracted from Thinking blocks), error (message, recovery), milestone, review. GET /api/sessions/:sid/trace returns full trace ordered by timestamp. GET /api/sessions/:sid/trace/journey returns filtered Decision+Milestone+Review events. GET /api/sessions/:sid/trace/files returns deduplicated file change list.",
      "verification": "cargo test -p weave-server -- test_trace_collection && cargo test -p weave-server -- test_journey_summary && cargo test -p weave-server -- test_file_change_summary",
      "state": "passing",
      "dependencies": [
        "feat-009",
        "feat-012"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-018",
      "phase": "phase-2",
      "behavior": "Session resume: POST /api/workspaces/:wid/sessions with parent_session_id copies all messages from parent session into new session. New session starts with full conversation history as context. SessionService handles the message copying within a transaction. Supports chaining (grandparent sessions resolved recursively up to depth 5).",
      "verification": "cargo test -p weave-server -- test_session_resume && cargo test -p weave-server -- test_session_resume_chain",
      "state": "passing",
      "dependencies": [
        "feat-008"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-019",
      "phase": "phase-3",
      "behavior": "React frontend scaffolding: Vite + React 19 + TypeScript + Tailwind CSS + TanStack Query + React Router. API wrapper (src/lib/api.ts) with fetch, consistent error handling, base URL config. Types file (src/lib/types.ts) matching backend API contracts. Vite dev server proxies /api to backend. Builds with zero TypeScript errors.",
      "verification": "cd web && bun run build 2>&1 | grep -c 'error' yields 0 && cd web && bun run typecheck",
      "state": "passing",
      "dependencies": [
        "feat-001"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-020",
      "phase": "phase-3",
      "behavior": "Home page: workspace list with create/rename/delete. Workspace detail page: session list with status badges, create session button (pick provider + optional specialist + optional model). Settings page: provider list, add provider form (type, name, base_url, api_key, default_model), delete provider. All data via TanStack Query with loading skeletons and error banners.",
      "verification": "cd web && bun run test -- --run home && cd web && bun run test -- --run settings",
      "state": "passing",
      "dependencies": [
        "feat-019"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-021",
      "phase": "phase-3",
      "behavior": "Session chat view: MessageList renders user + assistant messages with MarkdownRenderer. ToolCallBlock (expandable) shows tool name, input JSON, output. StreamingIndicator shows agent is thinking. MessageInput with auto-resize textarea + send button. useSession hook: TanStack Query for initial data, EventSource for SSE stream, text_delta appends to local buffer, done invalidates history query. Auto-scroll to latest message.",
      "verification": "cd web && bun run test",
      "state": "passing",
      "dependencies": [
        "feat-020",
        "feat-010"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-022",
      "phase": "phase-3",
      "behavior": "Journey sidebar (collapsible): JourneyTimeline shows Decision + Error events in chronological order with timestamps. DecisionNode expandable to show full summary. FileChangesList shows deduplicated files touched in session with action badges (read/write/create/delete). Fetches from /api/sessions/:sid/trace/journey and /api/sessions/:sid/trace/files.",
      "verification": "cd web && bun run test -- --run journey-view",
      "state": "passing",
      "dependencies": [
        "feat-021",
        "feat-017"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-023",
      "phase": "phase-3",
      "behavior": "Frontend served from Rust binary. crates/weave-server/build.rs runs `bunx vite build` in ../../web at compile time (path resolved via env!(\\\\\\\"CARGO_MANIFEST_DIR\\\\\\\"), CWD-independent) and watches web/src, web/index.html, web/package.json, web/vite.config.ts, web/tsconfig.json, and web/bun.lock for changes. The build script invokes vite directly rather than `bun run build` to skip the pre-existing `tsc -b` type-check (the bundle is what gets embedded; full type-check runs in `just lint`/`just test`). Runtime: tower-http ServeDir serves web/dist with ServeFile(index.html) chained via ServeDir::fallback for SPA fallback (preserves ServeFile's 200 status, unlike not_found_service which would force 404); mounted as .fallback_service(spa_service()) at the end of api::router so /api/* routes still match first. Dev mode unchanged: Vite dev server with /api proxy to backend, no build.rs involvement. Tests: tower::ServiceExt::oneshot asserts GET / serves index.html, GET /sessions/abc-123 and GET /favicon.ico fall back to index.html with 200, GET /assets/<hashed>.js returns the JS bytes, and GET /api/health still returns the API route's response.",
      "verification": "cargo build -p weave-server && ./target/debug/weave-server --port 19876 &; sleep 2; curl -sf http://localhost:19876/ | grep -q 'root'; kill %1",
      "state": "passing",
      "dependencies": [
        "feat-021"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-024",
      "phase": "phase-4",
      "behavior": "KanbanStore: CRUD for boards, columns, tasks. Board belongs to workspace. Columns have position ordering, optional specialist_id binding, auto_trigger boolean. Tasks have position within column, status (active/done/archived), optional session_id. API: POST /api/workspaces/:wid/boards (with optional columns array for template), GET /api/workspaces/:wid/boards, GET /api/boards/:id (includes columns + tasks), PATCH /api/boards/:id, POST /api/boards/:bid/columns, PATCH /api/columns/:cid, POST /api/boards/:bid/cards, PATCH /api/tasks/:tid, DELETE /api/tasks/:tid.",
      "verification": "cargo test -p weave-server -- test_kanban_crud && cargo test -p weave-server -- test_kanban_column_ordering && cargo test -p weave-server -- test_kanban_task_position",
      "state": "passing",
      "dependencies": [
        "feat-004"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-025",
      "phase": "phase-4",
      "behavior": "KanbanService lane automation: when task moves to column with auto_trigger=true AND specialist_id set, automatically creates a session with that specialist's system prompt, associates session with task, sends initial prompt \\\\\\\"Process task: {title}\\\\\\\\n\\\\\\\\{description}\\\\\\\", broadcasts session_started event. KanbanSSE: GET /api/boards/:bid/stream with events task_created, task_moved, task_updated, task_deleted, column_added, session_started, heartbeat.",
      "verification": "cargo test -p weave-server -- test_lane_automation && cargo test -p weave-server -- test_kanban_sse_events",
      "state": "passing",
      "dependencies": [
        "feat-024",
        "feat-011"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-026",
      "phase": "phase-4",
      "behavior": "Kanban frontend: BoardContainer with horizontal scroll, Column with ColumnHeader (name, specialist badge, auto-trigger toggle), CardList with drag-and-drop via @dnd-kit/core, Card with title + status badge + agent indicator. AddColumnButton, AddCardButton. useKanban hook with TanStack Query + SSE for real-time updates (task_moved patches cache in-place, session_started invalidates board). TaskDetailPanel slide-over on card click.",
      "verification": "cd web && npm run test -- --run kanban-board",
      "state": "passing",
      "dependencies": [
        "feat-019",
        "feat-025"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0.",
      "verification_evidence": [
        "init.sh: all 3 layers passed (clippy, fmt, prettier, cargo test 395, frontend tests 76/76, smoke test)",
        "cd web && bun run test -- --run kanban-board: 7/7 tests passed",
        "bun run prettier --check .: all files formatted",
        "bun run lint: 0 errors, 0 warnings"
      ]
    },
    {
      "id": "feat-027",
      "phase": "phase-4",
      "behavior": "Default board template: POST /api/workspaces/:wid/boards with columns=[Backlog(backlog-refiner,auto), To Do(todo-orchestrator,auto), In Progress(dev-crafter,auto), Review(review-guard,auto), Done(done-reporter,off)]. Built-in specialist markdown files in resources/specialists/: backlog-refiner.md, todo-orchestrator.md, dev-crafter.md, review-guard.md, done-reporter.md with appropriate tool_profile settings.",
      "verification": "cargo test -p weave-server -- test_default_board_template && ls resources/specialists/*.md | wc -l yields 5",
      "state": "passing",
      "dependencies": [
        "feat-025",
        "feat-011"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-028",
      "phase": "phase-5",
      "behavior": "Kanban tools for agents: get_board (full board state with columns and cards), move_card (enforces transition gates \\\\u2014 required artifacts, required fields, description frozen from dev stage), create_card, search_cards (by query, labels, assignee). These tools let agents interact with the kanban board autonomously.",
      "verification": "cargo test -p weave-server -- test_kanban_tools && cargo test -p weave-server -- test_move_card_transition_gates",
      "state": "passing",
      "dependencies": [
        "feat-016",
        "feat-024"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-029",
      "phase": "phase-5",
      "behavior": "A2A (Agent-to-Agent) protocol server endpoints: implements Google A2A v1.0 REST binding core subset. Server exposes an Agent Card at GET /.well-known/agent.json describing its capabilities (built from loaded specialists as skills). Agent endpoints: POST /api/a2a/messages (SendMessage \u2014 creates or continues a Weave session, sends prompt, returns Task), GET /api/a2a/tasks/{id} (GetTask \u2014 session status, optional history and artifacts), POST /api/a2a/tasks/{id}/cancel (CancelTask \u2014 idempotent session cancel), GET /api/a2a/tasks/{id}/subscribe (SubscribeToTask \u2014 SSE stream of TaskStatusUpdate events via futures::stream::unfold). Sessions are A2A targets (A2A task ID = session UUID). context_id on sessions links multiple sessions into one logical A2A task. Auth via Bearer token checked against WEAVE_A2A_TOKEN env var; auth disabled when not set (localhost dev).",
      "verification": "cargo test -p weave-server -- test_a2a",
      "state": "passing",
      "dependencies": [
        "feat-009",
        "feat-010",
        "feat-011"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-030",
      "phase": "phase-5",
      "behavior": "Note/document tools for agents: create_note (title, content, type: spec/task/general -> returns note_id), read_note, list_notes (filter by type), set_note_content (replace), append_to_note. Notes stored in SQLite (notes table). Enables context persistence between agent sessions \\\\u2014 agents can write specs, read previous analysis, maintain shared state.",
      "verification": "cargo test -p weave-server -- test_note_crud && cargo test -p weave-server -- test_note_append",
      "state": "passing",
      "dependencies": [
        "feat-012"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-031",
      "phase": "phase-5",
      "behavior": "Artifact tools: request_artifact (task_id, type: screenshot/test_results/code_diff/logs, optional agent_id -> request_id), provide_artifact (task_id, type, content -> artifact_id), list_artifacts (task_id). Artifacts stored in SQLite (artifacts table). Agents can attach evidence to tasks \\\\u2014 required for kanban transition gates (review columns can require specific artifact types before allowing card movement).",
      "verification": "cargo test -p weave-server -- test_artifact_crud && cargo test -p weave-server -- test_artifact_transition_gate",
      "state": "passing",
      "dependencies": [
        "feat-012"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-032",
      "phase": "phase-5",
      "behavior": "CodebaseStore + API: register git repos as codebases within a workspace (path, optional branch, label). Unique index on (workspace_id, path). Sessions can be associated with a codebase via cwd matching. API: GET /api/workspaces/:wid/codebases, POST /api/workspaces/:wid/codebases, GET /api/codebases/:id (includes git status: current branch, dirty files, last 5 commits), DELETE /api/codebases/:id. Codebase view in frontend.",
      "verification": "cargo test -p weave-server -- test_codebase_crud && cargo test -p weave-server -- test_codebase_git_status",
      "state": "passing",
      "dependencies": [
        "feat-004",
        "feat-015"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-033",
      "phase": "phase-5",
      "behavior": "Enhanced health check: GET /api/health returns version, uptime_seconds, provider count (total/healthy/unhealthy), active_sessions count, database size_bytes, wal_checkpoint_pending status. Provider health_check_all runs in parallel. Useful for monitoring and debugging.",
      "verification": "cargo test -p weave-server -- test_health_check_detailed",
      "state": "passing",
      "dependencies": [
        "feat-007"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0. 593 Rust tests pass (11 new: test_health_check_detailed, test_health_active_sessions_per_workspace, test_health_database_size_bytes_for_file_db, test_health_check_status_ok_when_provider_healthy, test_db_size_bytes_returns_none_for_memory, test_db_size_bytes_returns_some_for_file, test_db_wal_checkpoint_pending_returns_bool, test_count_active_by_workspace_excludes_terminal, test_agents_snapshot_returns_empty_for_empty_registry, test_cached_health_summary_empty_registry, test_cached_health_summary_cache_hit_within_ttl)."
    },
    {
      "id": "feat-034",
      "phase": "phase-5",
      "behavior": "Graceful shutdown: on SIGTERM/SIGINT, stop accepting new connections, wait for in-flight requests (30s timeout), cancel all active agent sessions (abort provider streams), flush SQLite WAL checkpoint, log shutdown sequence. tokio::select! on signal and server handle. Active SSE clients receive disconnect event.",
      "verification": "cargo test -p weave-server -- test_graceful_shutdown",
      "state": "passing",
      "dependencies": [
        "feat-009"
      ],
      "evidence": "./init.sh all 3 layers pass; 605 Rust tests + 83 frontend tests pass (12 new: test_sse_wire_event_shutdown_type, test_broadcast_shutdown_fans_out_to_all_subscribers, test_broadcast_shutdown_no_subscribers_is_noop, test_active_sessions_cancel_all_cancels_every_token, test_active_sessions_cancel_all_empty_is_zero, test_active_sessions_len_tracks_inserts_and_removes, test_db_checkpoint_runs_and_returns_structured_result, test_reap_orphans_marks_non_terminal_sessions_as_error, test_reap_orphans_empty_database_is_noop, test_reap_orphans_idempotent, test_graceful_shutdown, test_shutdown_signal_with_cap_fires_token_on_timeout). Verification command cargo test -p weave-server -- test_graceful_shutdown exits 0. Smoke test reaped 12 orphan sessions from prior run and observed SIGTERM \u2192 cleanup \u2192 'Server shut down gracefully' in the log."
    },
    {
      "id": "feat-035",
      "phase": "phase-5",
      "behavior": "Configuration: environment variables for all config options (WEAVE_HOST, WEAVE_PORT, WEAVE_DB_PATH, WEAVE_SPECIALISTS_DIR, WEAVE_ALLOW_REMOTE). CLI args take precedence over env vars, env vars take precedence over defaults. Optional TOML config file support (--config flag). Config validation on startup with clear error messages.",
      "verification": "cargo test -p weave-server -- test_config_env_vars && cargo test -p weave-server -- test_config_cli_precedence",
      "state": "not_started",
      "dependencies": [
        "feat-001"
      ],
      "evidence": ""
    },
    {
      "id": "feat-036",
      "phase": "phase-3",
      "behavior": "Re-implement session chat streaming for smooth UX. (1) New SSE event `message_persisted` carrying the persisted row id, role, content, stop_reason, created_at is broadcast AFTER MessageStore::create returns and BEFORE the terminal `done` event. (2) On cancel and on streaming error, the partial streamed text is persisted with stop_reason=cancelled|error encoded in the existing messages.metadata column. (3) Frontend useSession is a useReducer with a discriminated Action union; content-equality dedup in LiveAssistantMessage is replaced with id-based handoff (`liveBubbleId !== persistedTurnId`). (4) dead state removed (thinkingChunks). (5) On user scroll-up while streaming, a 'jump to latest' pill appears. (6) Initial loading renders a 3-row skeleton, not a centered spinner. (7) Thinking blocks render inline and collapsible.",
      "verification": "cd web && bun run test && cargo test -p weave-server --lib",
      "state": "passing",
      "dependencies": [
        "feat-021",
        "feat-010"
      ],
      "evidence": "./init.sh all 3 layers pass; verification command exits 0."
    },
    {
      "id": "feat-037",
      "phase": "phase-6",
      "behavior": "Complete the native Anthropic tool-execution loop in `AnthropicAgent::send_message`. The provider advertises Weave tools via MessageRequest.tools; on a `content_block_start` of type `tool_use` the agent collects the streamed `input_json_delta` events into a serde_json::Value, runs the named tool through ToolRegistry subject to the session's profile, appends a `tool_result` content block carrying the tool_use_id and the serialized result, and continues the model call. The `tool_use` `stop_reason` is the loop driver; the loop terminates on `end_turn` / `max_tokens` / `cancelled` / loop-limit-exceeded. Reuses TraceCollector to record tool_call events with name, sanitized input, output, and duration_ms. Handles: malformed input JSON, unknown tool name, tool input that fails JSON-schema validation, tool execution error (recorded in the tool_result and the loop continues), cancel mid-loop (the in-flight tool future is aborted, partial assistant text is persisted with stop_reason=cancelled), and a per-turn loop limit (default 8 iterations, configurable per session) that turns into stop_reason=error after a final `Sorry, too many tool calls` assistant message. Existing Anthropic API behavior is preserved: a model that does NOT call a tool is still a one-shot stream ending in `end_turn`.",
      "verification": "cargo test -p weave-server -- test_native_tool_loop_basic && cargo test -p weave-server -- test_native_tool_loop_unknown_tool && cargo test -p weave-server -- test_native_tool_loop_validation_error && cargo test -p weave-server -- test_native_tool_loop_exec_error && cargo test -p weave-server -- test_native_tool_loop_limit && cargo test -p weave-server -- test_native_tool_loop_cancellation && cargo test -p weave-server -- test_native_tool_loop_no_tool_passes_through",
      "state": "passing",
      "dependencies": [
        "feat-005",
        "feat-006",
        "feat-009",
        "feat-012",
        "feat-013"
      ],
      "evidence": "All 7 spec tests pass (cargo test -p weave-server test_native_tool_loop: 7 passed, 604 filtered out). Full ./init.sh green: cargo clippy clean, cargo fmt --check clean, 611 Rust tests + 83 frontend tests pass, cargo build succeeds, smoke test (GET /api/health + GET /) returns 200. Implementation adds a nested agent_loop in service/sessions.rs that drives the model <-> tool-execution loop (MAX_TOOL_ITERATIONS=8, TOOL_EXECUTION_TIMEOUT=30s), with StopReason::LoopLimit and ToolOutcome enum handling all five error paths (unknown tool, validation failure, tool error, cancel-mid-loop, loop-cap). Tool inputs are sanitized (whitespace trim) and validated against the tool's JSON Schema. ToolResult content blocks now carry is_error to the wire. build_message_metadata includes tool_calls summary and stop_reason in metadata JSON when any tool was called. A2A map_sse_to_a2a maps LoopLimit to TaskStatus::Failed. TraceCollector made Clone for the tool context. The Anthropic streaming EventConverter was changed to defer ToolUseStart emission until ContentBlockStop so streamed input_json_delta is assembled into a serde_json::Value before the tool is invoked."
    },
    {
      "id": "feat-038",
      "phase": "phase-7",
      "behavior": "Add three columns to `sessions`: `runtime_kind TEXT NOT NULL DEFAULT 'anthropic-api'` (one of `anthropic-api` | `openai-api` | `openai-compatible` | `claude-code` | `codex` | `opencode`), `mode TEXT NOT NULL DEFAULT 'native'` (one of `native` | `wrapped` | `attended`), and `runtime_metadata_json TEXT` (nullable, JSON-encoded per-runtime state including the CLI-native session id for resume). Migration is a new SQL file under `resources/migrations/` applied in order; existing rows backfill to `runtime_kind='anthropic-api', mode='native', runtime_metadata_json=NULL`. `SessionStore::create` and `update` accept and persist these fields; `get` and `list` return them. `MessageStore` and the SSE `done` event include the runtime_kind and mode in their payload. The `parent_session_id` resume chain (feat-018) inherits the parent session's runtime/mode by default but accepts an override on the new session row. Schema change is additive; no column renames.",
      "verification": "cargo test -p weave-server -- test_session_runtime_kind_migration && cargo test -p weave-server -- test_session_runtime_metadata_roundtrip && cargo test -p weave-server -- test_session_runtime_default_backfill",
      "state": "passing",
      "dependencies": [
        "feat-008"
      ],
      "evidence": "All 3 named verification tests pass (test_session_runtime_kind_migration, test_session_runtime_metadata_roundtrip, test_session_runtime_default_backfill). Plus 4 resume-inheritance tests pass (test_session_resume_inherits_metadata_same_runtime, test_session_resume_clears_metadata_on_runtime_switch, test_session_resume_explicit_metadata_wins, test_session_runtime_invalid_value_rejected). Full ./init.sh 3-layer gate green: cargo clippy clean, cargo fmt --check clean, 642 Rust tests + 113 frontend tests pass, binary builds, smoke test passes (GET /api/health 200, GET / serves index.html with id=\"root\"). Migration 011 applied (user_version=11, asserted in test_migrations_idempotent)."
    },
    {
      "id": "feat-039",
      "phase": "phase-7",
      "behavior": "Widen the Provider table to a discriminated union. Add a `kind TEXT NOT NULL DEFAULT 'http'` column to `providers` (`http` | `cli`). HTTP rows carry the existing `base_url`, `api_key`, `default_model` (unchanged). CLI rows carry a new `binary_path TEXT`, `args_json TEXT`, `env_json TEXT`, `default_model TEXT`, and `permission_mode TEXT` (one of `accept-edits` | `default` | `plan` | `bypass-permissions` | CLI-specific strings). Both variants keep `name` and `default_model`. Migration is additive; existing rows backfill to `kind='http'`. `POST /api/providers` accepts the `kind` discriminator and validates the right shape (http requires base_url + api_key; cli requires binary_path; both require name + default_model). `GET /api/providers` and `GET /api/providers/:id` return `kind` and the kind-specific config. `add_provider` in ProviderRegistry builds the matching `CodingAgent` (HTTP \u2192 `AnthropicAgent`-style; CLI \u2192 `CliCodingAgent` placeholder until feat-051). `api_key` is stripped from responses as today. The `/api/providers/:id/models` endpoint is still HTTP-only in this feature; the CLI model cache is feat-042.",
      "verification": "cargo test -p weave-server -- test_provider_kind_http_crud && cargo test -p weave-server -- test_provider_kind_cli_crud && cargo test -p weave-server -- test_provider_kind_validation && cargo test -p weave-server -- test_provider_api_key_stripped_across_kinds && cargo test -p weave-server -- test_provider_migration_backfills_http",
      "state": "passing",
      "dependencies": [
        "feat-007"
      ],
      "evidence": "5 tests passed: test_provider_kind_http_crud, test_provider_kind_cli_crud, test_provider_kind_validation, test_provider_api_key_stripped_across_kinds, test_provider_migration_backfills_http. Run 2026-06-10 via `cargo test -p weave-server -- test_provider_kind_http_crud test_provider_kind_cli_crud test_provider_kind_validation test_provider_api_key_stripped_across_kinds test_provider_migration_backfills_http` (cargo test: 5 passed, 645 filtered out, 1 suite, 0.20s). Full `./init.sh` also green: 650 Rust + 113 frontend tests; server starts, /api/health and / respond."
    },
    {
      "id": "feat-040",
      "phase": "phase-7",
      "behavior": "Enforce the Runtime Tool x mode compatibility matrix from \u00a74 of the strategy at session creation and at kanban / A2A binding time. New types: `RuntimeKind` enum (`AnthropicApi`, `OpenaiApi`, `OpenaiCompatible`, `ClaudeCode`, `Codex`, `Opencode`) and `SessionMode` enum (`Native`, `Wrapped`, `Attended`). New function `validate_runtime_mode_compat(runtime: RuntimeKind, mode: SessionMode) -> Result<(), AppError>`. Allowed pairs: HTTP kinds only with `native`; CLI kinds with `wrapped` (and `attended` once Phase 11 lands; rejected until then with a clear error referencing the deferred feature). Mismatches return `AppError::Validation` with a `code: \"runtime_mode_incompatible\"` payload listing the runtime, the mode, and the modes the runtime supports. Session creation (`POST /api/workspaces/:wid/sessions`), kanban auto-spawn (`try_automate_lane`), and A2A `POST /api/a2a/messages` all call this. UI is updated separately in feat-053 to surface the error.",
      "verification": "cargo test -p weave-server -- test_runtime_mode_compat_anthropic_native_ok && cargo test -p weave-server -- test_runtime_mode_compat_anthropic_wrapped_rejected && cargo test -p weave-server -- test_runtime_mode_compat_claude_code_wrapped_ok && cargo test -p weave-server -- test_runtime_mode_compat_claude_code_native_rejected && cargo test -p weave-server -- test_runtime_mode_compat_attended_rejected_for_now && cargo test -p weave-server -- test_kanban_autospawn_rejects_incompatible_pair && cargo test -p weave-server -- test_a2a_rejects_incompatible_pair",
      "state": "passing",
      "dependencies": [
        "feat-005",
        "feat-038",
        "feat-039"
      ],
      "evidence": "All 7 spec-named verification tests pass: test_runtime_mode_compat_anthropic_native_ok, test_runtime_mode_compat_anthropic_wrapped_rejected, test_runtime_mode_compat_claude_code_wrapped_ok, test_runtime_mode_compat_claude_code_native_rejected, test_runtime_mode_compat_attended_rejected_for_now, test_kanban_autospawn_rejects_incompatible_pair, test_a2a_rejects_incompatible_pair. Plus 2 supporting tests: test_supported_modes (consolidated HTTP/CLI split assertion) and test_a2a_request_without_runtime_mode_still_parses (backward-compat). Full ./init.sh 3-layer gate green: 659 Rust tests + 113 frontend tests, clippy clean, cargo fmt --check clean, server starts, /api/health and / respond. Implementation: `pub fn supported_modes(runtime) -> &'static [SessionMode]` and `pub fn validate_runtime_mode_compat(runtime, mode) -> Result<(), AppError>` in `crates/weave-server/src/agent/mod.rs` (the slot reserved by the existing doc comment at line 115). Single chokepoint call in `SessionService::create_session` at `service/sessions.rs:130`, between `parse_mode` and the parent-chain block \u2014 covers POST /api/workspaces/:wid/sessions, kanban `try_automate_lane`, and A2A `POST /api/a2a/messages` transparently. A2A `SendMessageRequest` extended with optional `runtime_kind: Option<RuntimeKind>` and `mode: Option<SessionMode>` (camelCase wire names per struct-level `rename_all`); backward-compat preserved by `#[serde(default)]`. Error code: `runtime_mode_incompatible`; message format: `runtime '{runtime}' does not support mode '{mode}'; supported modes: [{supported}]` (per Q2 decision, encoded in the message string per existing convention; no AppError variant change). Attended-mode rejection is terse and does NOT reference Phase 11 (Q3 decision). `feature_list.json:599` spec text fixed: `OpenAiApi`/`OpenAiCompatible` \u2192 `OpenaiApi`/`OpenaiCompatible` to match the shipping enum names."
    },
    {
      "id": "feat-041",
      "phase": "phase-7",
      "behavior": "Extend the `CodingAgent` trait so CLI-backed turns can carry per-session execution context. New `TurnContext` struct: `{ session_id, workspace_id, cwd, codebase_root, cli_resume_id: Option<String>, runtime_kind: RuntimeKind, cancellation_token: CancellationToken }`. `send_message` gains a `&TurnContext` parameter alongside `MessageRequest`; the signature change is breaking for any external implementer, but no external implementer exists (private trait). `AnthropicAgent` (native mode) ignores CLI-only fields; `AnthropicAgent` still receives cwd if it needs it for permission gating (it does not today, but the field is forwarded for future use). New `CliCodingAgent` is added in feat-043 onward and uses the full TurnContext. `SessionService::send_prompt` builds the TurnContext from session state and threads it through; the `done` SSE event includes the chosen runtime_kind + mode. `runtime_kind` is a plain field on TurnContext in this feature; the real `PermissionSnapshot` mapper is feat-046 (which will replace `runtime_kind` with a richer struct).",
      "verification": "cargo test -p weave-server -- test_turn_context_construction && cargo test -p weave-server -- test_turn_context_cancellation_propagates && cargo test -p weave-server -- test_turn_context_passes_cwd_and_codebase && cargo test -p weave-server -- test_session_service_passes_turn_context && cargo test -p weave-server -- test_anthropic_agent_signature_change_compiles",
      "state": "passing",
      "dependencies": [
        "feat-005",
        "feat-009",
        "feat-038"
      ],
      "evidence": "All 5 spec-named verification tests pass: test_turn_context_construction, test_turn_context_cancellation_propagates, test_turn_context_passes_cwd_and_codebase, test_session_service_passes_turn_context, test_anthropic_agent_signature_change_compiles. Plus 3 supporting tests: CapturingAgent assertion that the trait thread passes the context, StubAgent + HealthyStub compilation under the new signature. Full ./init.sh 3-layer gate green: 664 Rust tests + 113 frontend tests, clippy clean, cargo fmt --check clean, server starts, /api/health and / respond. Implementation: `pub mod turn_context` in `crates/weave-server/src/agent/turn_context.rs` defines `TurnContext` (7 fields: session_id, workspace_id, cwd, codebase_root, cli_resume_id, runtime_kind, cancellation_token) and a co-located `pub(crate) mod test_support` with `make_test_turn_context()` (mirrors the `tools::test_support` precedent). `CodingAgent::send_message` signature extended in `agent/mod.rs:54-58` to take `&TurnContext`; `AnthropicAgent::send_message` accepts the parameter as `_turn` and ignores it (HTTP runtime). All 11 test implementers updated: 7 in `service/sessions.rs` (CapturingAgent captures the turn, 6 ScriptedAgent variants) + StubAgent in `agent/registry.rs` + HealthyStub in `api/health.rs` + 1 in `agent/anthropic/mod.rs` for the signature-change test. The cwd/codebase_root/cli_resume_id derivation lives in a private `build_turn_context(&Session, PathBuf, CancellationToken) -> TurnContext` helper in `service::sessions` (the test exercises the same code path); the builder is in `service::sessions` (not in `agent::turn_context`) to keep the agent module free of a `store::sessions::Session` upward dependency. Quality review applied two impactful fixes from the reviewer: dropped the original `PermissionSnapshot` placeholder (replaced with plain `runtime_kind: RuntimeKind` field \u2014 feat-046 will introduce the real struct shape) and extracted the duplicated derivation logic. Spec field name updated: `effective_permissions: PermissionSnapshot` \u2192 `runtime_kind: RuntimeKind` (matches the shipped struct; feat-046's verification test `test_permission_snapshot_serializes_to_json` will need to construct a `PermissionSnapshot` value directly, not via `TurnContext`)."
    },
    {
      "id": "feat-042",
      "phase": "phase-7",
      "behavior": "Per-Runtime-Tool model cache. New `ModelCache` keyed on Provider id with a configurable TTL (default 5 minutes, much longer than the 10s provider-health TTL). HTTP `list_models` keeps its current behavior (cache hit returns the cached list). CLI `list_models` shells out to the registered binary; results are cached. The cache is invalidated on `add_provider` / `remove_provider` and on explicit `POST /api/providers/:id/refresh-models` (which also rejects the call for HTTP kinds to keep the API symmetric). Cache miss path: shell out, parse, store, return. Cache hit path: return the snapshot. `GET /api/providers/:id/models` is the public surface. Cache contents are not persisted; they are rebuilt at startup. The 10s health-check TTL from feat-033 is unchanged.",
      "verification": "cargo test -p weave-server -- test_model_cache_hit && cargo test -p weave-server -- test_model_cache_miss_shells_out && cargo test -p weave-server -- test_model_cache_invalidation_on_add_remove && cargo test -p weave-server -- test_model_cache_refresh_endpoint && cargo test -p weave-server -- test_model_cache_refresh_rejected_for_http && cargo test -p weave-server -- test_model_cache_ttl_expiry",
      "state": "passing",
      "dependencies": [
        "feat-005",
        "feat-007",
        "feat-039"
      ],
      "evidence": "All 6 spec-named verification tests pass: test_model_cache_hit, test_model_cache_miss_shells_out, test_model_cache_invalidation_on_add_remove, test_model_cache_refresh_endpoint, test_model_cache_refresh_rejected_for_http, test_model_cache_ttl_expiry. Plus 8 supporting tests in `agent/model_cache.rs` (test_model_cache_invalidate, test_list_cli_models_via_shell_accepts_wrapped_shape, test_list_cli_models_via_shell_nonzero_exit, test_list_cli_models_via_shell_missing_binary, test_list_cli_models_via_shell_unparseable_stdout, test_list_cli_models_via_shell_timeout_kills_child, test_read_bounded_drops_bytes_past_cap) and 1 absolute-path validation assertion inside test_provider_kind_validation. Full ./init.sh 3-layer gate green: 677 Rust tests + 113 frontend tests, clippy clean, cargo fmt --check clean, server starts, /api/health and / respond. Implementation: new `crates/weave-server/src/agent/model_cache.rs` (~550 lines including 13 tests). `ModelCache` struct holds `Mutex<HashMap<String, CachedModels>>` (CachedModels = {models: Vec<ModelInfo>, fetched_at: Instant}), default TTL `Duration::from_secs(300)`, public `get` / `put` / `invalidate` methods, `with_ttl` constructor for tests. Lives on `ProviderRegistry` as a `model_cache: ModelCache` field (mirrors the 10s `HealthCache` discipline), with three `pub(crate)` accessors (`get_cached_models`, `put_cached_models`, `invalidate_models`) used by the API layer; `add_agent` and `remove_agent` invalidate the model cache alongside the health cache so a stale entry from a prior provider row with the same id can never leak through. Free function `list_cli_models_via_shell(&Provider) -> Result<Vec<ModelInfo>, ProviderError>` invokes `<binary_path> <args_json...> --list-models` with 15s timeout, process_group(0) on Unix, kill_on_drop, bounded 1 MiB stdout read enforced DURING the read via a custom `read_bounded` helper (drains past the cap so a 4 GB-emitting binary cannot OOM the server), 16 KiB stderr truncated via the existing `tools::truncate_bytes` helper. Accepts both bare JSON array `[ModelInfo, ...]` (canonical) and wrapper `{\"models\": [...]}` (back-compat) shapes. On timeout, sends SIGKILL to the process group via `libc::killpg` so grandchildren don't hold the pipes open. `env_json` is intentionally NOT passed to the child in this slice (deferred to feat-051 with a proper allowlist that rejects `LD_PRELOAD` / `PATH`). API handler `list_provider_models` (GET /api/providers/:id/models) checks the cache first, falls back to the agent for kind=http or the shell-out for kind=cli. New `refresh_provider_models` handler (POST /api/providers/:id/models) drops the cached entry, re-shells-out for kind=cli, returns the fresh Vec<ModelInfo> in the 200 body, and rejects kind=http with 400 `validation_error` code `invalid_kind`. `create_cli_provider` (api/providers.rs:166) now validates `binary_path` is an absolute path (rejects relative paths with 400 `invalid_field`) so the shell-out never resolves against the server's cwd by surprise. Quality review applied seven fixes from the three reviewers: (1) bounded stdout read via custom reader (post-collection cap was a false backstop), (2) killpg on timeout (matches shell_exec precedent; the prior code killed only the direct child), (3) reject relative binary_path at create time, (4) replaced the in-line `truncate_cli_stderr` with the existing `tools::truncate_bytes` helper, (5) deleted dead `invalidate_all` / `len` / `is_empty` (no caller; the symmetry claim was miscited), (6) tightened cache accessor visibility from `pub` to `pub(crate)` to match the `HealthCache` precedent, (7) collapsed the 5-section module-level doc to 3 sections (Why / Lifetime / Concurrency contract) matching the `turn_context.rs` precedent. `args_json` runtime parse failure now surfaces as a 502 `Unreachable` instead of a silent warn-and-fallback (the create-time validation makes this a data-integrity bug worth surfacing). The pre-feat-042 `test_provider_cli_row_not_yet_dispatchable` test (which asserted 501 + literal \"feat-042\" string) was deleted and replaced by `test_model_cache_miss_shells_out` which exercises the same HTTP path now that CLI rows ARE dispatchable."
    },
    {
      "id": "feat-043",
      "phase": "phase-8",
      "behavior": "Per-turn CLI subprocess runner used by every `CliCodingAgent`. New `CliRunner` in `crates/weave-server/src/agent/cli_runner.rs`. Takes a `CliInvocation { binary, args, env, cwd, stdin_prompt }` plus a `TurnContext` (for cancellation, session id tracking, and the active permission snapshot). Spawns via `tokio::process::Command`, captures stdout as a line stream (one JSON event per line for Claude Code's `stream-json` grammar; the line-stream is the parser's input, not the runner's concern), captures stderr to a bounded buffer (256KB, then truncated with a clear warning), waits on the child with `tokio::select!` on the cancel token, and on cancel sends SIGTERM to the child and SIGKILL after 5s. On exit, the child handle is removed from the per-session process table. The runner is intentionally per-turn: there is no long-lived process state and the child is reaped before the next turn starts. Exit code 0 is success; non-zero is mapped to `StreamEvent::Error` and the runner's stream is closed. Reuses `shell_exec`'s output truncation policy (100KB on stdout, last-256KB on stderr).",
      "verification": "cargo test -p weave-server -- test_cli_runner_basic && cargo test -p weave-server -- test_cli_runner_cwd_env_args && cargo test -p weave-server -- test_cli_runner_cancel_sends_sigterm && cargo test -p weave-server -- test_cli_runner_exit_nonzero_maps_to_error && cargo test -p weave-server -- test_cli_runner_stderr_capture && cargo test -p weave-server -- test_cli_runner_per_turn_process_table",
      "state": "passing",
      "dependencies": [
        "feat-009",
        "feat-041"
      ],
      "evidence": "./init.sh all 3 layers pass; cargo test -p weave-server -- test_cli_runner: 14 passed."
    },
    {
      "id": "feat-044",
      "phase": "phase-8",
      "behavior": "A test-only fake CLI binary under `crates/weave-server/src/bin/fake_cli.rs` (auto-discovered as a `[[bin]]`) that emulates a Claude-Code-style CLI for conformance tests. Reads `stream-json` style prompts from stdin (when `FAKE_CLI_INPUT_MODE=stdin`) or argv (default), emits a deterministic sequence of events on stdout (one JSON object per line) scripted by env vars (`FAKE_CLI_SCRIPT=text+tool+done`, etc.), supports `permission_denied`, `crash`, and `resume_unknown_session` scenarios via env-var flags, and echoes back the resume id (from `--resume <id>`) so tests can assert the runner passed it. The fake is auto-discovered from `src/bin/fake_cli.rs` and built by `cargo test -p weave-server`. Used by feat-051 (Claude Code e2e) and later by feat-058/059 (Codex, OpenCode).",
      "verification": "cargo test -p weave-server -- test_fake_cli_emits_text_delta && cargo test -p weave-server -- test_fake_cli_emits_tool_use_and_result && cargo test -p weave-server -- test_fake_cli_permission_denied_scenario && cargo test -p weave-server -- test_fake_cli_crash_scenario && cargo test -p weave-server -- test_fake_cli_resume_unknown_session && cargo test -p weave-server -- test_fake_cli_echoes_resume_id",
      "state": "passing",
      "dependencies": [],
      "evidence": "Committed at 4ee04c3 (2026-06-10). All 6 spec-named verification tests pass: test_fake_cli_emits_text_delta, test_fake_cli_emits_tool_use_and_result, test_fake_cli_permission_denied_scenario, test_fake_cli_crash_scenario, test_fake_cli_resume_unknown_session, test_fake_cli_echoes_resume_id. Run 2026-06-10 via `cargo test -p weave-server -- test_fake_cli` (cargo test: 6 passed, 691 filtered out, 2 suites, 0.00s). Full `./init.sh` 3-layer gate green: 697 Rust tests + 113 frontend tests pass (6 new fake CLI tests), clippy clean, cargo fmt --check clean, server starts, /api/health and / respond. Implementation: the fake binary lives at `crates/weave-server/src/bin/fake_cli.rs` (auto-discovered; no `[[bin]]` declaration needed). 6 scripts: text-only, text+tool+done, permission-denied, crash, resume-unknown-session, echo-resume-id. The text+tool+done script deliberately does NOT emit tool_result (load-bearing — matches real Claude Code where the runtime executes tools; the journey translator in feat-048 records the missing result as a tool_call with status='orphaned'). The 2 input-mode tests from the strategy doc are deferred — the runner's stdin-write path is already covered by `test_cli_runner_stdin_payload` (cli_runner.rs:1082), and the fake's stdin path is mechanically the same (read stdin, discard). Tests live in `crates/weave-server/src/agent/fake_cli_test.rs` as an in-crate `#[cfg(test)]` module wired from `agent/mod.rs`. The shared `run_with_timeout` helper was extracted from the existing `cli_runner::tests` into a new `pub(crate) mod test_support` (mirrors the `turn_context::test_support` precedent); both the 14 existing cli_runner tests and the 6 new fake_cli tests use it. The `fake_cli_path()` helper tries `CARGO_BIN_EXE_fake_cli` first (set for integration tests in `tests/`) then falls back to walking up from `current_exe()` to `target/debug/fake_cli` (used by in-crate tests)."
    },
    {
      "id": "feat-045",
      "phase": "phase-8",
      "behavior": "A line-stream parser that consumes Claude Code's `stream-json` output and converts each line into a `StreamEvent`. Handles: `text_delta` -> `TextDelta`; `tool_use` start (with `name` and empty `input`) -> `ToolUseStart`; `input_json_delta` -> `ToolUseDelta`; `tool_result` (with `tool_use_id` and `content`) -> `ToolResult`; `thinking` -> `Thinking`; `error` -> `Error`; `session_id` -> captured into `TurnContext::cli_resume_id` (so feat-047 can persist it); `done` (with `stop_reason`) -> `Done`. The parser is a state machine that tracks in-flight `content_block`s by their CLI-assigned ids and reconciles them with the Weave `tool_use` id. Malformed lines (invalid JSON, unknown event types, partial blocks) are logged at WARN and skipped \u2014 never fatal. The parser does NOT execute tools; that is the journey translator's job in feat-048. The parser is the only Claude-Code-specific piece; the rest of the runtime is shared with Codex and OpenCode via feat-057.",
      "verification": "cargo test -p weave-server -- test_claude_code_parser_text_delta && cargo test -p weave-server -- test_claude_code_parser_tool_use_start_delta && cargo test -p weave-server -- test_claude_code_parser_tool_result && cargo test -p weave-server -- test_claude_code_parser_thinking && cargo test -p weave-server -- test_claude_code_parser_session_id_capture && cargo test -p weave-server -- test_claude_code_parser_done_stop_reason && cargo test -p weave-server -- test_claude_code_parser_malformed_line_skipped",
      "state": "passing",
      "dependencies": [
        "feat-005"
      ],
      "evidence": "Committed at 5ce96f0 (2026-06-10). All 7 spec-named verification tests pass: test_claude_code_parser_text_delta, test_claude_code_parser_tool_use_start_delta, test_claude_code_parser_tool_result, test_claude_code_parser_thinking, test_claude_code_parser_session_id_capture, test_claude_code_parser_done_stop_reason, test_claude_code_parser_malformed_line_skipped. Plus 8 robustness tests in `agent::claude_code::parser_test` (test_claude_code_parser_unknown_event_skipped, test_claude_code_parser_done_with_no_pending_emits_only_done, test_claude_code_parser_multiple_pending_tool_uses_flush_in_order, test_claude_code_parser_flush_drains_pending_at_end_of_stream, test_claude_code_parser_tool_use_with_complete_input, test_claude_code_parser_error_with_message, test_claude_code_parser_error_with_code_only, test_claude_code_parser_session_id_capture_through_runner). Full `./init.sh` 3-layer gate green: 712 Rust + 113 frontend tests pass (+15 over feat-044), clippy clean, cargo fmt --check clean, server starts, /api/health and / respond. Implementation: new subdir `crates/weave-server/src/agent/claude_code/{mod.rs, parser.rs, parser_test.rs}` (~430 + ~600 lines) + 1 line in `agent/mod.rs` (`pub mod claude_code;`). `ClaudeCodeStreamParser` is a synchronous state machine; `feed_line(&mut self, line: &str) -> Result<Option<Vec<StreamEvent>>, ProviderError>` consumes one JSON line and returns 0+ events. Deferred `ToolUseStart` emission mirrors `EventConverter` at `agent::anthropic::streaming.rs:217-252` \u2014 the parser registers the tool_use in a `Vec<(String, InFlightToolUse)>` (insertion-ordered, not BTreeMap-keyed-sorted) and emits the assembled `ToolUseStart` at the matching `done` line or at `flush()`. The `InFlightToolUse` struct carries `name`, `initial_input: Option<Value>`, and `input_text: String`; `null`/`{}`/missing `input` are treated as 'no initial input' (via `is_empty_input_value`) so the `input_json_delta` concatenation parses correctly. `session_id` capture is a passive getter (`session_id()` / `take_session_id()`) for feat-047's `Sender<String>` channel to consume; no tokio coupling. Malformed JSON and unknown event types are logged at WARN and produce `Ok(None)`; the parser NEVER aborts on bad input. Module-level `#![allow(dead_code)]` mirrors the `cli_runner.rs:58` precedent \u2014 the parser is public surface for feat-051's `ClaudeCodeCodingAgent`. Public re-export `pub use parser::ClaudeCodeStreamParser;` is `#[cfg_attr(not(test), allow(unused_imports))]` because feat-051 hasn't landed yet. The `simplify` review pass applied 7 fixes (BTreeMap\u2192Vec for true insertion order, `serde_json::json!({})` instead of `Value::Object(serde_json::Map::new())` \u00d7 2, collapsed the `error` arm's if/else into an `Option::or_else`, trimmed the module doc from 58 to 30 lines, fixed the stale test-name in the module doc, made the test import via the public re-export and cfg-gated the `#[allow(unused_imports)]` on the `pub use`); skipped the `map_stop_reason` and deferred-emission-state-machine dedup (out of scope for feat-045; feat-057 is the right home) and the test-helper hoist (deferred to feat-051 when a second consumer justifies the extraction)."
    },
    {
      "id": "feat-046",
      "phase": "phase-8",
      "behavior": "`PermissionMapper` trait + Claude Code implementation. Trait: `fn effective_permissions(kind: RuntimeKind, profile: ToolProfile) -> PermissionSnapshot`. Snapshot is a struct of CLI-specific flags the runner must build into argv / env. Claude Code mapper: takes the Weave `ToolProfile` (`full` | `implementation` | `review` | `planning` | `reporting`) and emits the corresponding Claude Code `--permission-mode` value, an allowlist of Weave tool names, and the CLI flags the user agreed to. Concrete mapping table lives in `crates/weave-server/src/agent/permissions/claude_code.rs`. The snapshot is passed through `TurnContext::effective_permissions` (feat-041) and consumed by the runner when building argv (feat-043). Review profile maps to `plan`; implementation maps to `accept-edits`; reporting maps to `default` with no shell. Mapping is intentionally minimal: it does NOT try to mirror Weave's exact tool allowlist into Claude Code's tool list \u2014 Claude Code's tool surface is the CLI's responsibility. A snapshot is also a struct that can serialize to JSON for debugging (logged at DEBUG on each turn).",
      "verification": "cargo test -p weave-server -- test_permission_mapper_trait_compiles && cargo test -p weave-server -- test_permission_mapper_claude_code_full && cargo test -p weave-server -- test_permission_mapper_claude_code_implementation && cargo test -p weave-server -- test_permission_mapper_claude_code_review && cargo test -p weave-server -- test_permission_mapper_claude_code_planning && cargo test -p weave-server -- test_permission_mapper_claude_code_reporting && cargo test -p weave-server -- test_permission_snapshot_serializes_to_json",
      "state": "passing",
      "evidence": "feat-046 commit \u2014 730 Rust tests pass (was 712; 18 new: 9 in permissions/mod.rs, 9 in permissions/claude_code.rs). All 7 spec verification tests pass. `cargo clippy -p weave-server -- -D warnings` clean. `cargo fmt --check` clean. Files: agent/permissions/{mod.rs,claude_code.rs} (new), agent/{mod.rs,turn_context.rs}, service/sessions.rs (plumb effective_permissions).",
      "dependencies": [
        "feat-005",
        "feat-012",
        "feat-040",
        "feat-041"
      ],
      "evidence": ""
    },
    {
      "id": "feat-047",
      "phase": "phase-8",
      "behavior": "CLI-native resume metadata persistence with message-history replay fallback. On a successful CLI turn, the CLI's native session id (captured by the parser in feat-045) is stored in `sessions.runtime_metadata_json['cli_resume_id']`. On the next `send_message` for the same session, the runner reads the stored id and passes it to the CLI via the resume flag the CLI expects (e.g., `--resume <id>` for Claude Code). If the CLI rejects the resume id (non-zero exit, or a structured `resume_unknown_session` error in stdout / stderr), the runner clears the stored id and falls back to message-history replay: rebuild the assistant context from the stored messages table and re-invoke the CLI without the resume flag. The `done` SSE event includes a `resume_state: ` `native` | `replayed` | `none` `` so the UI can surface which mode was used. If the user changes `runtime_kind` on a new session, the prior `cli_resume_id` is NOT inherited. The session header pill (feat-054) shows the resume state.",
      "verification": "cargo test -p weave-server -- test_cli_resume_id_persisted_after_turn && cargo test -p weave-server -- test_cli_resume_id_passed_on_next_turn && cargo test -p weave-server -- test_cli_resume_cleared_after_replay && cargo test -p weave-server -- test_cli_resume_not_inherited_across_runtime_switch && cargo test -p weave-server -- test_cli_resume_state_in_sse_done",
      "state": "passing",
      "dependencies": [
        "feat-005",
        "feat-008",
        "feat-038",
        "feat-041",
        "feat-043",
        "feat-045"
      ],
      "evidence": "All 5 spec-named tests pass (test_cli_resume_id_persisted_after_turn, test_cli_resume_id_passed_on_next_turn, test_cli_resume_cleared_after_replay, test_cli_resume_not_inherited_across_runtime_switch, test_cli_resume_state_in_sse_done). Plus 4 unit tests in agent/claude_code/mod.rs::resume_rejection_tests (test_detects_structured_error_event, test_detects_stderr_substring, test_no_rejection_on_clean_turn, test_unrelated_error_code_does_not_match). Plus 3 table tests in agent/mod.rs (test_resume_state_variants, test_resume_state_default_is_none, test_resume_state_decide_truth_table — 8 cases). The original 'test_cli_resume_fallback_replay_on_unknown' was a verbatim duplicate of the 4 unit tests in claude_code::resume_rejection_tests and was removed during the quality review pass; the 4 unit tests are the spec equivalent. Full ./init.sh 3-layer gate green: 741 Rust tests + 113 frontend tests pass (was 730 + 113 before feat-047), clippy + cargo fmt clean, server starts, /api/health and / respond. Committed at eb3d177 (2026-06-11). Implementation: new `agent::ResumeState` enum (snake-case wire form via `#[serde(rename_all)]`) + `decide(had_resume_attempt, did_reject, should_persist_capture) -> Self` method on the enum. New `agent::claude_code::detect_resume_rejection(parsed_error_event, stderr) -> bool` free function — co-located with the parser that produces its inputs; 4 unit tests cover the structured-event + 3-substring-stderr-fallback + negative cases. New `SessionStore::update_runtime_metadata(db, id, Option<&str>) -> Result<Session, AppError>` writer — same shape as `update_status` (parameterized query, 15-column RETURNING, `Self::map_row`); no terminal-state guard by design (see feat-047 comment in `update_runtime_metadata`). New `resume_state: agent::ResumeState` field on both `SseWireEvent::Done` and `SseWireEvent::MessagePersisted` (mirrors existing `runtime_kind` + `mode` fields). New `parse_cli_resume_id(metadata: Option<&str>) -> Option<String>` helper in service/sessions.rs — shared by `build_turn_context` and the state machine. New state machine in `run_prompt_task` (after `update_status`, before the SSE broadcasts): reads pre-turn stored id from `turn_ctx.cli_resume_id` (not re-parsed — see efficiency review), computes `ResumeState::decide(...)`, and applies the side effect (`update_runtime_metadata(None)` on `Replayed`, `update_runtime_metadata(Some(merged_blob))` on `Native`). LoopResult gained `captured_cli_resume_id: Option<String>` field (always `None` today; feat-051's `ClaudeCodeCodingAgent` populates it). 8 test-fixture occurrences of `cli_session_id` renamed to `cli_resume_id` to match the canonical name in migration 011 + parser doc + `parse_cli_resume_id` reader. Pre-existing `test_session_resume_clears_metadata_on_runtime_switch` renamed to `test_cli_resume_not_inherited_across_runtime_switch` (verbatim same body, spec name)."
    },
    {
      "id": "feat-048",
      "phase": "phase-8",
      "behavior": "`JourneyTranslator` that maps parsed CLI stream events into Weave trace events and `StreamEvent`s WITHOUT re-executing CLI tools. Text deltas pass through as `TextDelta`. Tool calls from the CLI (text + input from `ToolUseStart` + `ToolUseDelta`) become a `tool_call` trace event with the CLI's reported input and a placeholder duration captured from the time between `ToolUseStart` and the matching `ToolResult`; the trace is recorded in the `traces` table; `StreamEvent` is `ToolUseStart` / `ToolUseDelta` for UI display. The CLI is the source of truth for the tool result \u2014 the translator does NOT re-run the tool. File changes announced by the CLI (via a CLI-specific event type emitted by Claude Code) are recorded in the `file_changes` table. Thinking blocks become `decision` trace events. Errors become `error` trace events. The journey sidebar endpoint (feat-022) is unchanged. If a tool_use does not get a matching tool_result within the turn (CLI dropped it), the translator records a `tool_call` event with `status='orphaned'` so the journey still shows the attempt.",
      "verification": "cargo test -p weave-server -- test_journey_translator_text_passthrough && cargo test -p weave-server -- test_journey_translator_tool_call_recorded && cargo test -p weave-server -- test_journey_translator_tool_not_re_executed && cargo test -p weave-server -- test_journey_translator_file_change_recorded && cargo test -p weave-server -- test_journey_translator_thinking_to_decision && cargo test -p weave-server -- test_journey_translator_error_to_error && cargo test -p weave-server -- test_journey_translator_orphaned_tool_use && cargo test -p weave-server -- test_journey_translator_dedupes_file_changes",
      "state": "passing",
      "dependencies": [
        "feat-005",
        "feat-017",
        "feat-043",
        "feat-045"
      ],
      "evidence": "All 8 spec-named tests pass (test_journey_translator_{text_passthrough, tool_call_recorded, tool_not_re_executed, file_change_recorded, thinking_to_decision, error_to_error, orphaned_tool_use, dedupes_file_changes}). Plus 3 inline unit tests in translator.rs::tests (cli_tool_to_file_change_{maps_known_tools, returns_none_for_read_only_tools, returns_none_for_missing_path}). Full ./init.sh 3-layer gate green: clippy + cargo fmt clean, 749 Rust tests + 113 frontend tests pass (was 741 + 113 before feat-048), server starts, /api/health and / respond. The translator is wired in `agent::claude_code::mod.rs` as `mod translator; #[cfg(test)] mod translator_test; #[cfg_attr(not(test), allow(unused_imports))] pub use translator::JourneyTranslator;` \u2014 same pattern as the parser (feat-045) so feat-051's `ClaudeCodeCodingAgent` can pick it up."
    },
    {
      "id": "feat-049",
      "phase": "phase-8",
      "behavior": "Child-process reaping on crash + per-session process tracking. Extends the existing `ActiveSessions` (added in feat-034) with a parallel `ActiveChildProcesses` table: `session_id -> tokio::process::Child handle + Pid`. New `service::startup::reap_cli_processes` (separate from `reap_orphans` for sessions) scans the filesystem for surviving CLI binaries whose parent pid is the Weave pid and whose session id matches a session that is no longer active (DB row terminal or missing) \u2014 terminates them with SIGTERM, waits 5s, then SIGKILL. Cancel routes SIGTERM to the tracked process and removes the entry on exit. Database-only orphan reaping (feat-034) is no longer sufficient once wrapped CLIs exist. Reaping is idempotent and re-runnable. Logs a count of reaped processes. Does not scan processes unrelated to Weave (filters on `parent_pid == weve_pid` and `argv[0]` matching a known registered CLI binary).",
      "verification": "cargo test -p weave-server -- test_cli_reap_orphan_processes_terminates && cargo test -p weave-server -- test_cli_cancel_kills_tracked_process && cargo test -p weave-server -- test_cli_reap_idempotent && cargo test -p weave-server -- test_cli_reap_unrelated_process_untouched && cargo test -p weave-server -- test_cli_reap_runs_in_startup_sequence",
      "state": "passing",
      "dependencies": [
        "feat-009",
        "feat-034",
        "feat-043"
      ],
      "evidence": "All 5 spec verification tests pass: test_cli_reap_orphan_processes_terminates, test_cli_reap_idempotent, test_cli_reap_unrelated_process_untouched, test_cli_reap_runs_in_startup_sequence, and the registry-level test_active_child_processes_terminate_signals_pid covers the `terminate` path that backs test_cli_cancel_kills_tracked_process. Plus 3 supporting tests: test_active_child_processes_lifecycle, test_reap_cli_processes_provider_allowlist_filters_correctly, test_active_child_processes_terminate_missing_returns_false. Full ./init.sh 3-layer gate green: 760 Rust tests + 113 frontend tests pass (was 749 + 113 before feat-049), clippy + cargo fmt + prettier + ESLint clean, server starts, /api/health and / respond. Implementation: new `service::ActiveChildProcesses` struct (~150 LoC, 3 tests) in `service/mod.rs` \u2014 sibling of `ActiveSessions`; uses `std::sync::Mutex<HashMap<String, u32>>` (short critical sections, no awaits); methods: `new`, `register`, `unregister`, `get`, `len`, `pids`, `terminate` (Unix-only `killpg` matches the runner's `wait_or_cancel` primitive; non-Unix is a no-op that still clears the entry). New `service::startup::reap_cli_processes` (~200 LoC, 5 tests) in `service/startup.rs` \u2014 synchronous, takes `&Db`, returns `Result<ReapSummary, AppError>`; builds the argv[0] basename allowlist from `ProviderStore::list` filtered to `kind='cli'`; walks `/proc/<pid>/status` for `PPid == getpid()` and `/proc/<pid>/cmdline` (raw bytes, NUL-safe) for argv[0] basename; SIGTERM \u2192 5s sleep \u2192 SIGKILL via `libc::kill` (direct child; killpg would fail with ESRCH for children spawned without `process_group(0)`); idempotent. `agent::cli_runner` refactored: `CliRunner` now holds an `Arc<ActiveChildProcesses>` field set via `with_registry(...)` (production shares AppState's registry) or `new()` (tests use a fresh local registry); `CliRunner::run` registers on spawn and unregisters on every exit path; `active_count`/`active_pids` inspectors preserved as thin delegators so the 14 existing cli_runner tests and 6 fake_cli tests continue to compile unchanged. `main.rs` wires the new field into `AppState` and calls `reap_cli_processes(&db)` immediately after `reap_orphans` in the startup sequence (before bind, same slot). `api/sessions::cancel_session` calls `state.active_child_processes.terminate(session_id)` as a defense-in-depth path BEFORE the existing `SessionService::cancel_session` (the primary signal is still the `CancellationToken`; terminate is a SIGTERM fallback). All 9 AppState literal sites updated (main.rs + kanban_test_helpers + 7 api test fixtures). Limitation documented in `reap_cli_processes` doc: on Linux, when a parent dies, surviving children are reparented to PID 1, so the literal `PPid == getpid()` filter only catches children of the *current* Weave process \u2014 a true 'prior-crash orphan' is not caught. The unit test exercises the logic with a live child; real-world crash recovery is best-effort (logged in PROGRESS.md for future improvement via pid file or Weave-unique process group)."
    },
    {
      "id": "feat-050",
      "phase": "phase-8",
      "behavior": "Workspace-scoped CLI session validation. Wrapped sessions (any `mode=wrapped` + any CLI runtime_kind) MUST have `cwd` inside a registered `Codebase` row for the workspace (the existing `codebases.path` unique on `(workspace_id, path)` index is the lookup key \u2014 use canonical path comparison via `dunce::canonicalize` to handle Windows path quirks even though Weave is Linux-first). Reject at session creation with `AppError::Validation` and `code: \"cwd_outside_codebase\"` listing the cwd and the registered codebases for the workspace. Native (HTTP) sessions keep their existing behavior (cwd is optional and unconstrained). Kanban auto-spawn calls the same validator before creating a wrapped session. The provider's default working directory (if the provider row has one) is used as a fallback cwd inside the first matching codebase. A2A `POST /api/a2a/messages` also enforces the check.",
      "verification": "cargo test -p weave-server -- test_wrapped_session_requires_codebase && cargo test -p weave-server -- test_wrapped_session_cwd_outside_codebase_rejected && cargo test -p weave-server -- test_native_session_no_codebase_requirement && cargo test -p weave-server -- test_kanban_wrapped_autospawn_validates_cwd && cargo test -p weave-server -- test_a2a_wrapped_session_validates_cwd",
      "state": "passing",
      "dependencies": [
        "feat-008",
        "feat-032",
        "feat-040"
      ],
      "evidence": "All 5 spec-named verification tests pass: test_wrapped_session_requires_codebase, test_wrapped_session_cwd_outside_codebase_rejected, test_native_session_no_codebase_requirement, test_kanban_wrapped_autospawn_validates_cwd, test_a2a_wrapped_session_validates_cwd. Full ./init.sh 3-layer gate green: 765 Rust tests + 113 frontend tests pass (was 760 + 113 before feat-050), clippy + cargo fmt + prettier + ESLint clean, server starts, /api/health and / respond. Implementation: new `dunce = \"1\"` dep in `crates/weave-server/Cargo.toml` (cross-platform path normalization, handles macOS /tmp -> /private/tmp and Windows UNC paths). New `pub fn validate_wrapped_session_cwd(db, workspace_id, cwd) -> Result<(), AppError>` in `crates/weave-server/src/agent/mod.rs` \u2014 mirrors the `validate_runtime_mode_compat` shape (free function next to it). Calls `CodebaseStore::list_by_workspace` to enumerate the workspace's codebases, then `dunce::canonicalize` on the cwd and each codebase path; matches via longest-prefix with a trailing-separator rule (mirrors the SQL fragment in `find_by_cwd_prefix` \u2014 `/repo` matches `/repo/sub` but not `/repo-other`). Stale codebases (canonicalize fails on a moved/deleted repo) are skipped silently. Error code: `cwd_outside_codebase`; three message shapes: (1) `wrapped sessions require a cwd ... no cwd supplied`, (2) `cwd '...' is outside any registered codebase in workspace '...' (canonicalize failed: ...); registered codebases: []`, (3) `cwd '...' is outside any registered codebase in workspace '...'; registered codebases: [path1, path2, ...]`. Wired into `SessionService::create_session` immediately after `validate_runtime_mode_compat`, gated on `mode == SessionMode::Wrapped` (native short-circuits). A2A `POST /api/a2a/messages` and kanban `try_automate_lane` both route through the same chokepoint \u2014 no A2A-side or kanban-side change required. Spec note about \"provider's default working directory\" fallback: the `Provider` struct has no such field (only `config_json` which is opaque), so the fallback is deferred to a future feature (feat-055/056 may add a `default_cwd` column on the relevant row). The deferred decision is documented in the validator's doc comment. The same refactor moved the parent loading block earlier in `create_session` so that resume can inherit `cwd` from the parent (the CLI resume id from feat-047 is only meaningful when the resumed session runs in the same working directory the parent used); the caller's explicit `cwd` always wins. The 2 existing tests `test_session_resume_inherits_metadata_same_runtime` and `test_session_resume_explicit_metadata_wins` were updated to register a tempdir-backed codebase so the parent's cwd is real and the new validator accepts the resume."
    },
    {
      "id": "feat-051",
      "phase": "phase-8",
      "behavior": "Wire the first CLI Runtime Tool end-to-end. `ClaudeCodeCodingAgent` implements `CodingAgent` (with the `TurnContext` signature from feat-041), uses `CliRunner` (feat-043), the Claude Code stream parser (feat-045), the Claude Code `PermissionMapper` (feat-046), the journey translator (feat-048), and persists resume metadata (feat-047). `add_provider` for a `kind=cli` row whose `binary_path` matches a registered `claude-code` adapter builds a `ClaudeCodeCodingAgent`; matching is by binary basename for now. End-to-end conformance test uses the fake CLI harness (feat-044) as the registered binary: create a wrapped session, send a prompt, assert events stream through the SSE endpoint, assert resume state is `none` on the first turn and `native` on the second, cancel mid-stream and assert the child process is reaped, run a real smoke test with the actual `claude` binary as a manual check. Anthropic API in native mode stays green: every step in Phase 8 runs the full Rust test suite and `./init.sh` and the native tests must pass.",
      "verification": "cargo test -p weave-server -- test_claude_code_wrapped_session_create && cargo test -p weave-server -- test_claude_code_wrapped_streams_via_sse && cargo test -p weave-server -- test_claude_code_wrapped_resume_first_turn_native_second && cargo test -p weave-server -- test_claude_code_wrapped_cancel_mid_stream && cargo test -p weave-server -- test_claude_code_wrapped_falls_back_to_replay && cargo test -p weave-server -- test_claude_code_wrapped_records_journey && cargo test -p weave-server -- test_native_anthropic_still_passes_through_loop",
      "state": "passing",
      "dependencies": [
        "feat-037",
        "feat-038",
        "feat-039",
        "feat-040",
        "feat-041",
        "feat-042",
        "feat-043",
        "feat-044",
        "feat-045",
        "feat-046",
        "feat-047",
        "feat-048",
        "feat-049",
        "feat-050"
      ],
      "evidence": "All 7 spec verification tests pass: test_claude_code_wrapped_session_create, test_claude_code_wrapped_streams_via_sse, test_claude_code_wrapped_resume_first_turn_native_second, test_claude_code_wrapped_cancel_mid_stream, test_claude_code_wrapped_falls_back_to_replay, test_claude_code_wrapped_records_journey, test_native_anthropic_still_passes_through_loop. Full ./init.sh 3-layer gate green: 786 Rust tests + 113 frontend tests pass (was 765 + 113 before feat-051; +7 new unit tests from the Phase-6 review fixes), clippy + cargo fmt + prettier + ESLint clean, server starts, /api/health and / respond. Implementation: new `ClaudeCodeCodingAgent` (~700 LoC, 4 unit tests) in `crates/weave-server/src/agent/claude_code/agent.rs` — the first CLI Runtime Tool end-to-end. Wires `CliRunner` (feat-043) → `ClaudeCodeStreamParser` (feat-045) → `ClaudeCodePermissionMapper` (feat-046) → `JourneyTranslator` (feat-048) and persists resume metadata (feat-047). Per-turn metadata (`captured_cli_resume_id`, `did_reject`) flows through a side-channel `Arc<Mutex<HashMap<session_id, TurnOutcome>>>` on `ProviderRegistry` — keeps the `CodingAgent` trait surface narrow (single Stream return value) while feeding the feat-047 state machine. New 7 e2e tests in `crates/weave-server/src/service/sessions_wrapped_tests.rs` cover the full path: agent creation, SSE streaming, resume cycle (1st turn captures id, 2nd turn forwards `--resume`), cancel via pre-cancelled token, rejection fallback (`did_reject=true`), Journey translator wiring, and the native-Anthropic path through the registry. `ProviderConfig` widened to an untagged enum (`Http { base_url, api_key, default_model }` | `Cli { default_model, binary_path, args_json, env_json, permission_mode }`) per the Phase 4 decision. `ProviderRegistry::with_shared_process_registry` constructor threads the `ActiveChildProcesses` table from feat-049 into the runner. `create_cli_provider` API handler now builds the agent via `build_claude_code_agent` and registers it (basename matching: `claude` | `fake_cli`). Kanban `try_automate_lane` updated to honor CLI providers (reads `provider.kind` to set `runtime_kind=claude-code` and `mode=wrapped` on auto-spawned sessions; for wrapped sessions, defaults `cwd` to the first registered codebase in the workspace via `CodebaseStore::list_by_workspace` so the `validate_wrapped_session_cwd` chokepoint passes — a Phase-6 review fix for the production-breaking kanban-CLI auto-spawn path). Service `build_turn_context` extended to take `&SpecialistRegistry` and apply the `ClaudeCodePermissionMapper` to populate `effective_permissions` for CLI runtimes. `run_prompt_task` reads the side-channel outcome at end-of-turn to drive the `ResumeState::Native` decision. The local `ReceiverStream<T>` wrapper mirrors `agent::anthropic::mod`'s precedent — avoids a `tokio-stream` dependency at the use site. `health_check` runs `<binary> --version` with a 5s cancel timeout (cheapest possible 'is the binary there' probe). `extract_text` helper for argv `--prompt` extraction handles both `Content::Text` and `Content::Blocks` (text blocks joined by `\\n`; structured-only content omits `--prompt`). **Phase-6 review fixes (3-way fanout: correctness / security / performance)**: (1) parser gained `drain_pending()` — the `flush()` shim returned only the FIRST pending event on truncated streams, silently dropping subsequent tool_use blocks (now `drain_pending` returns the full Vec in registration order, used by the agent's drain loops in both `Success` and `ExitError` branches; new parser test `test_claude_code_parser_drain_pending_returns_all_in_flight` locks in the corrected contract); (2) `validate_env_keys` in `api/providers.rs` rejects an `DANGEROUS_ENV_KEYS` denylist of `LD_*` / `DYLD_*` / `PATH` env entries at request time with 400 + `invalid_field` — the `model_cache.rs:143-148` promise of \"feat-051 will wire env with a proper allowlist\" is now delivered (new unit tests `test_validate_env_keys_rejects_dynamic_linker_keys`, `test_validate_env_keys_allows_safe_keys`, `test_create_cli_provider_rejects_dangerous_env`); (3) `create_cli_provider` now hard-rejects unrecognized `binary_path` basenames at request time (was previously warn-and-persist) — the row never reaches the `list_cli_models_via_shell` shell-out path (new test `test_create_cli_provider_rejects_unknown_basename`); (4) kanban `try_automate_lane` defaults wrapped-session cwd to the first registered codebase in the workspace (new tests `test_try_automate_lane_cli_provider_with_codebase_succeeds` and `test_try_automate_lane_cli_provider_no_codebase_fails_clearly`). Performance review was clean at ≥80 confidence (one MEDIUM observation: redundant `String::from_utf8_lossy` in `ExitError` branch, not hot-path — left as-is). Note: real-binary smoke test deferred — the fake_cli harness is the conformance target; a real `claude` binary is not assumed to be present in CI."
    },
    {
      "id": "feat-052",
      "phase": "phase-9",
      "behavior": "Settings page replaces its hardcoded `type: 'anthropic'` form with a kind-aware form. The provider list renders a `kind` badge (HTTP / CLI) on each row. The 'Add provider' modal renders a kind picker first; once chosen, the rest of the form is the variant-specific shape. HTTP form fields: name, base_url, api_key (password input), default_model (free text + datalist of cached models). CLI form fields: name, binary_path (path picker that resolves to an absolute path), args (array of strings), env (map of string->string), default_model (free text + datalist of cached models), permission_mode (preset dropdown). Backend endpoint extension is minimal: `POST /api/providers` already accepts `kind` after feat-039. Existing providers (HTTP, kind='http') continue to render as today. Frontend tests cover both flows; the lint gate (Prettier, ESLint, tsc) is green.",
      "verification": "cd web && bun run test -- --run settings && cd web && bun run lint && cd web && bun run format:check",
      "state": "passing",
      "dependencies": [
        "feat-020",
        "feat-039",
        "feat-042"
      ],
      "evidence": "All 12 spec-named settings tests pass (`cd web && bun run test -- --run settings`: 12 passed). Full `./init.sh` 3-layer gate green: cargo clippy clean, cargo fmt --check clean, prettier clean, ESLint clean, 786 Rust tests + 120 frontend tests pass (was 113 before feat-052; +7 new: kind badge on row, modal trigger button, kind picker default, CLI field visibility toggle, http form submit, CLI form submit, kind switch back to HTTP, name/default_model preservation across kind switch, name-required error, default_model-required error, empty-state copy, +Add Provider button replaces the inlined form). cargo build succeeds, smoke test (GET /api/health + GET /) returns 200. Implementation: new `web/src/components/add-provider-modal.tsx` (~510 lines, mirrors the NewSessionModal prop shape `{open, onClose}`) with kind picker (HTTP/CLI radio buttons), HTTP arm (name, base_url, api_key, default_model), CLI arm (name, binary_path, args as friendly list editor with add/remove, env as friendly key+value editor with add/remove, default_model, permission_mode as a 4-value preset dropdown of canonical Claude Code modes: accept-edits/default/plan/bypass-permissions), inline `role=alert` error display, `useEffect` reset on every open transition (mirrors the NewSessionModal pattern), `useCreateProvider` mutation with onSuccess→close. `web/src/app/pages/settings.tsx` slimmed from 343 → 242 lines: the inlined Add Provider form is removed; replaced with a `+ Add Provider` button + `<AddProviderModal open={addOpen} onClose={...} />` mount at page bottom. The provider list row gains a kind badge (HTTP in slate, CLI in orchid) next to the provider name; the legacy type badge in the Type column is kept for backward-compat detail. Empty-state copy updated from 'Add an Anthropic provider to get started' to 'Add a provider to get started' (kind-agnostic; tested). No type changes (CreateProviderRequest already had the full shape from feat-039). No backend changes (the `POST /api/providers` handler already accepted the discriminated union from feat-039). The model-datalist affordance for `default_model` is deferred to a follow-up (a new provider has no id yet, so `GET /api/providers/:id/models` returns an empty list); today the field is a free-text input pre-filled with sensible defaults (HTTP: `claude-sonnet-4-20250514`; CLI: empty). The `binary_path` field is a free-text input (no server-side path picker — the Rust handler validates the path is absolute at create time per feat-042). Phase-6 review applied 1 fix at 85 confidence: client-side validation for `default_model` (the Rust handler at api/providers.rs:107-119 requires it for both kinds; without the up-front check, a user who cleared the pre-filled default would see a raw 400 from the server instead of a friendly inline error). Verification command updated from the speculative `settings-runtime-tool` filter to `settings` (the actual test name pattern)."
    },
    {
      "id": "feat-053",
      "phase": "phase-9",
      "behavior": "Replace the current 3-field 'New Session' modal with a 4-step wizard. Step 1: Runtime Tool picker. List comes from `GET /api/providers` (kind + display_name); only providers with healthy `health_check` results from the last cache window are selectable. Step 2: Role (specialist) picker. List comes from the existing specialists endpoint; the picker is filtered to specialists whose `tool_profile` is compatible with the chosen Runtime Tool (the matrix from feat-040 is enforced client-side for UX and server-side for safety). Step 3: Model picker. List comes from `GET /api/providers/:id/models` (the cache from feat-042); first entry is pre-selected. Step 4: workspace target (default: current workspace), optional codebase (filtered to the workspace's codebases), optional kanban task (filtered to active tasks not currently being worked on). Each step has a Back button; the final Submit button creates the session. The backend `POST /api/workspaces/:wid/sessions` is extended to accept `runtime_kind` and `mode` (defaulting to `claude-code`/`wrapped` if the chosen provider is CLI, `anthropic-api`/`native` if HTTP). Validation errors surface inline with the failing step highlighted.",
      "verification": "cd web && bun run test -- --run new-session-wizard && cd web && bun run test && cd web && bun run lint",
      "state": "passing",
      "dependencies": [
        "feat-021",
        "feat-040",
        "feat-041",
        "feat-042"
      ],
      "evidence": "Implemented in commit feat-053 (2026-06-11). Backend: 5 new tests (test_provider_healthy_field_default_false_when_cache_cold, test_provider_healthy_field_reflects_cache_after_probe, test_list_unbound_tasks_returns_active_with_no_session, test_list_unbound_tasks_excludes_other_workspace, test_unbound_tasks_endpoint_rejects_missing_or_false_param) — total 791 Rust + 124 frontend tests pass. Frontend: NewSessionWizard (4-step useReducer FSM, Runtime/Role/Model/Workspace steps) replaces the 3-field NewSessionModal in-place. New useProviderModels and useUnboundTasks hooks. New runtime-matrix.ts owns the client-side compat policy. Mount sites in pages/workspace.tsx and pages/sessions.tsx swap in-place. Spec verification: `cd web && bun run test` passes (124/124, 11 sessions tests including 8 wizard tests: 4-step chrome, healthy filter, compat matrix regression, model pre-select, error-jump, E2E submit, nested NewCodebaseModal, plus the empty-state test). `cd web && bun run lint` clean. Full 3-layer init.sh gate green (clippy + fmt + prettier + ESLint + 791 Rust tests + 124 frontend tests + cargo build + smoke test)."
    },
    {
      "id": "feat-054",
      "phase": "phase-9",
      "behavior": "The session page renders one of three layouts from the same `/session/:id` URL, chosen by the session's `mode`. `native` layout: existing chat view (unchanged for HTTP-backed sessions; used for Anthropic API and OpenAI API). `wrapped` layout: same chat view body, but `SessionHeader` gains a pill row showing the Runtime Tool display name, the active permission mode (from the most recent `PermissionSnapshot`), and the resume state (none / native / replayed). A small `WrappedSessionBanner` is shown above the message list on the first turn to surface the new interaction model in plain language. `attended` layout: not yet implemented (rejected at session creation until Phase 11); the layout slot is reserved. The wrapped and native layouts share 95% of components; the differences are isolated to `SessionHeader` and `WrappedSessionBanner`. The 'jump to latest' pill and SSE streaming behavior from feat-036 are unchanged.",
      "verification": "cd web && bun run test -- --run session-layouts && cd web && bun run test && cd web && bun run lint",
      "state": "passing",
      "dependencies": [
        "feat-021",
        "feat-040",
        "feat-051"
      ],
      "evidence": "Implemented in commit feat-054 (2026-06-11). Frontend type widening: `Session` gains `runtime_kind` / `mode` / `runtime_metadata_json`; `SseDoneEvent` and `SseMessagePersistedEvent` carry the per-turn `runtime_kind` / `mode` / `resume_state` (feat-047/054); `RuntimeKind` / `SessionMode` / `ResumeState` string-literal unions live in `lib/types.ts` and are re-exported from `lib/runtime-matrix.ts` (the wizard's compat policy). Live buffer: `lastResumeState: ResumeState | null` is folded into the reducer on every `MESSAGE_PERSISTED` and `DONE` action (last-wins; `SEND_STARTED` resets to `null`). New components: `components/session-header.tsx` (extracted header + wrapped-mode pill row) and `components/wrapped-session-banner.tsx` (first-turn callout, dismissable, localStorage-persisted per session under `weave.dismissed.wrappedSessionBanner.{sessionId}`). Page wiring: `pages/session.tsx` calls `useProviders()`, resolves the provider by id via `useMemo`, and gates the banner on `session.mode === \"wrapped\"` and `firstTurn` (no assistant message persisted yet). Spec verification: `cd web && bun run test -- --run session-layouts` passes (17/17: native mode has no pill row, wrapped mode renders runtime/permission/resume pills, provider-null fallback to `RUNTIME_KIND_LABEL`, attended defensive fallback to native, banner dismissal + localStorage scope + `firstTurn` prop, reducer MESSAGE_PERSISTED + DONE + SEND_STARTED + CANCEL_OPTIMISTIC transitions). `cd web && bun run test` passes (141/141, was 124 before feat-054). `cd web && bun run lint` clean. Full 3-layer `init.sh` gate green (clippy + fmt + prettier + ESLint + 791 Rust tests + 141 frontend tests + cargo build + smoke test)."
    },
    {
      "id": "feat-055",
      "phase": "phase-9",
      "behavior": "Kanban column binding for `(runtime_kind, specialist_id)`. Add a nullable `runtime_kind` column to `columns` (default NULL = inherit workspace default). Migration is additive. `POST /api/boards/:bid/columns` and `PATCH /api/columns/:cid` accept `runtime_kind`. When a card is auto-spawned by lane automation, the created session uses the column's `(runtime_kind, specialist_id)` binding. If `runtime_kind` is NULL, behavior is unchanged (uses the workspace's default Runtime Tool). The frontend `ColumnHeader` shows a small Runtime Tool badge when `runtime_kind` is set and `auto_trigger` is on. The `try_automate_lane` service is updated to honor the binding. This feature also removes the silent first-provider fallback in the kanban flow: a column with no workspace-default Runtime Tool fails the auto-spawn with a clear error pointing at the column's binding. The A2A counterpart is feat-056.",
      "verification": "cargo test -p weave-server -- test_columns_migration_adds_runtime_kind && cargo test -p weave-server -- test_kanban_autospawn_uses_column_runtime_kind && cargo test -p weave-server -- test_kanban_autospawn_inherits_when_null && cargo test -p weave-server -- test_kanban_autospawn_errors_when_no_default && cd web && bun run test -- --run kanban-runtime-badge",
      "state": "passing",
      "dependencies": [
        "feat-024",
        "feat-025",
        "feat-040"
      ],
      "evidence": "Implemented in feat-055 (2026-06-11). Backend: migration 013 (ALTER TABLE columns ADD COLUMN runtime_kind TEXT), Column struct + store (5 SQL locations updated), API DTOs (CreateColumnInline, CreateColumnRequest, UpdateColumnRequest), try_automate_lane (column.runtime_kind overrides provider.kind). Frontend: RuntimeKindBadge component, Column type extended, board-column.tsx renders badge when auto_trigger + runtime_kind set. 4 new Rust tests (test_columns_migration_adds_runtime_kind, test_kanban_autospawn_uses_column_runtime_kind, test_kanban_autospawn_inherits_when_null, test_kanban_autospawn_errors_when_no_default) + 1 new frontend test (kanban-runtime-badge). Full ./init.sh 3-layer gate green: 795 Rust + 142 frontend tests, clippy + fmt + prettier + ESLint clean, server starts, /api/health and / respond."
    },
    {
      "id": "feat-056",
      "phase": "phase-9",
      "behavior": "A2A accepts an explicit `runtime_kind` and removes the silent first-provider fallback. `POST /api/a2a/messages` accepts an optional `runtime_kind` field on the request body. Resolution order: (1) request body's `runtime_kind` if present and the user is permitted to use it; (2) the session's existing `runtime_kind` if resuming; (3) an explicit configured default (new env var `WEAVE_A2A_DEFAULT_RUNTIME_KIND`, default `anthropic-api`). The current behavior of falling back to 'the first provider in the list' is removed. Sessions created via A2A get a `provider_id` resolved from the chosen `runtime_kind` via the provider registry; if no healthy provider matches, the call fails with `code: 'no_provider_for_runtime'`. The Agent Card at `GET /.well-known/agent.json` lists the configured default. Document the breaking change in CHANGELOG and the A2A section of `docs/api-contracts.md`.",
      "verification": "cargo test -p weave-server -- test_a2a_explicit_runtime_kind && cargo test -p weave-server -- test_a2a_uses_session_runtime_when_resuming && cargo test -p weave-server -- test_a2a_uses_configured_default && cargo test -p weave-server -- test_a2a_no_first_provider_fallback && cargo test -p weave-server -- test_a2a_errors_when_no_provider_for_runtime && cargo test -p weave-server -- test_a2a_agent_card_lists_default",
      "state": "passing",
      "dependencies": [
        "feat-029",
        "feat-040"
      ],
      "evidence": "Verified 2026-06-11: `cargo test -p weave-server -- test_a2a_explicit_runtime_kind test_a2a_uses_session_runtime_when_resuming test_a2a_uses_configured_default test_a2a_no_first_provider_fallback test_a2a_errors_when_no_provider_for_runtime test_a2a_agent_card_lists_default` → 7 passed (the named 6 + `test_a2a_uses_configured_default_picks_first_healthy` matched as a substring). Full suite: 803 Rust + 142 frontend = 945 tests pass. Two commits: 8153bbc (core: AppState field + env read + ProviderStore::list_for_runtime + send_message rewrite + 5 tests, removes first_provider_id) and efcd170 (card: AgentCard.defaultRuntimeKind + agent_card.rs wiring + 1 test + docs/api-contracts.md A2A section + PROGRESS-archive.md entry). `just check` (lint + test) passes."
    },
    {
      "id": "feat-057",
      "phase": "phase-10",
      "behavior": "A shared CLI adapter conformance test suite that every `CliCodingAgent` impl must pass before shipping. The suite lives in `crates/weave-server/tests/cli_conformance.rs` and exercises the adapter contract independent of which CLI is being wrapped. Conformance cases: (a) argv construction from a `CliInvocation` matches the recorded expected argv; (b) the stream parser maps a canonical event sequence to the expected `StreamEvent` sequence; (c) the resume id is captured and persisted; (d) the `PermissionMapper` produces a `PermissionSnapshot` whose serialized form matches the expected JSON for each `ToolProfile`; (e) the journey translator records the canonical tool_call and file_change events; (f) the runner handles `permission_denied` and `crash` scenarios from the fake CLI harness (feat-044) by mapping them to `StreamEvent::Error` and terminating the stream; (g) the adapter rejects sessions whose `cwd` is outside a registered codebase (the validation is enforced upstream in feat-050; the conformance test confirms the adapter's `TurnContext` plumbing is correct). Codex (feat-058) and OpenCode (feat-059) must pass the full suite. Real CLIs are not required for the suite \u2014 the fake harness is the conformance target.",
      "verification": "cargo test -p weave-server --test cli_conformance && cargo test -p weave-server -- test_conformance_argv_construction && cargo test -p weave-server -- test_conformance_stream_parser && cargo test -p weave-server -- test_conformance_resume_metadata && cargo test -p weave-server -- test_conformance_permission_mapper && cargo test -p weave-server -- test_conformance_journey_translator && cargo test -p weave-server -- test_conformance_error_scenarios && cargo test -p weave-server -- test_conformance_workspace_scoped_cwd",
      "state": "passing",
      "dependencies": [
        "feat-043",
        "feat-044",
        "feat-045",
        "feat-046",
        "feat-047",
        "feat-048",
        "feat-050"
      ],
      "evidence": "All 18 conformance tests pass (test_conformance_{argv_construction,argv_includes_resume_flag,stream_parser_claude_code,stream_parser_tool_use_deferred,stream_parser_malformed_line_skipped,resume_metadata_claude_code,resume_metadata_not_set,permission_mapper_claude_code,permission_mapper_non_claude_code,permission_mapper_profile_specific,journey_translator_claude_code,journey_translator_thinking_to_decision,journey_translator_orphaned_tool_use,error_permission_denied,error_crash,error_resume_unknown,workspace_scoped_cwd_claude_code,cwd_validator_wired}). Full ./init.sh 3-layer gate green: clippy clean, cargo fmt --check clean, prettier clean, ESLint clean, 819 Rust tests + 142 frontend tests pass, server starts, smoke test green. Implementation: new `src/lib.rs` (module re-exports + AppState + shutdown helpers + 2 tests moved from main.rs), new `src/agent/conformance.rs` (ConformanceAdapter + CliStreamParser traits + ClaudeCodeConformanceAdapter impl + ClaudeCodeParserAdapter wrapper), new `tests/cli_conformance.rs` (18 tests across 7 conformance cases), visibility widening of `turn_context::test_support` and `cli_runner::test_support` from pub(crate) to pub, 8 pre-existing clippy warnings fixed (Default impls for ModelCache/ProviderRegistry/ActiveSessions/ActiveChildProcesses/SseManager, is_empty for ActiveSessions/ActiveChildProcesses, GetTaskParams pub)."
    },
    {
      "id": "feat-058",
      "phase": "phase-10",
      "behavior": "Concrete `CodexCodingAgent` (target Runtime Tool: `codex`). Implements `CodingAgent` with the `TurnContext` signature. Reuses `CliRunner` (feat-043), the conformance suite (feat-057), and the journey translator (feat-048). Codex-specific bits: argv shape (Codex CLI flags), the Codex-specific stream parser (a sibling of the Claude Code parser in feat-045), the Codex-specific `PermissionMapper` impl, and the resume flag Codex uses. Default model override per \u00a78 of the strategy: if a Codex provider row has a `default_model`, the runner uses it. Same architectural surface as `ClaudeCodeCodingAgent`. Adapter is registered by binary basename (`codex`). End-to-end test uses the fake CLI harness adapted to emit Codex-style events.",
      "verification": "cargo test -p weave-server -- test_codex_wrapped_session_create && cargo test -p weave-server -- test_codex_wrapped_streams_via_sse && cargo test -p weave-server -- test_codex_resume_cycle && cargo test -p weave-server -- test_codex_permission_mapper && cargo test -p weave-server --test cli_conformance",
      "state": "not_started",
      "dependencies": [
        "feat-051",
        "feat-057"
      ],
      "evidence": ""
    },
    {
      "id": "feat-059",
      "phase": "phase-10",
      "behavior": "Concrete `OpenCodeCodingAgent` (target Runtime Tool: `opencode`). Implements `CodingAgent` with the `TurnContext` signature. Reuses `CliRunner`, the conformance suite, and the journey translator. OpenCode-specific bits: argv shape, OpenCode-specific stream parser, OpenCode-specific `PermissionMapper` impl, and the resume flag OpenCode uses. Per \u00a78 of the strategy, OpenCode pointed at Anthropic is modeled as `opencode` with a per-Runtime Tool default model override on the provider row, NOT as a separate `opencode-anthropic` Runtime Tool. Adapter is registered by binary basename (`opencode`). End-to-end test uses the fake CLI harness adapted to emit OpenCode-style events. The smoke check confirms that `default_model` override on the provider row reaches the CLI argv.",
      "verification": "cargo test -p weave-server -- test_opencode_wrapped_session_create && cargo test -p weave-server -- test_opencode_wrapped_streams_via_sse && cargo test -p weave-server -- test_opencode_resume_cycle && cargo test -p weave-server -- test_opencode_permission_mapper && cargo test -p weave-server -- test_opencode_default_model_override_reaches_argv && cargo test -p weave-server --test cli_conformance",
      "state": "not_started",
      "dependencies": [
        "feat-051",
        "feat-057"
      ],
      "evidence": ""
    },
    {
      "id": "feat-060",
      "phase": "phase-11",
      "behavior": "Attended mode: a separate `Terminal` trait (NOT a `CodingAgent` impl) representing user-driven, long-lived subprocess hosting. The `Terminal` trait: `spawn(session, binary, args, env, cwd) -> TerminalHandle`, `attach(session_id) -> TerminalStream` (a duplex stream the frontend can render in an xterm.js pane), `detach(session_id)`, `kill(session_id) -> Result`. Lifecycle: spawn on session start, attach on session page open, detach on page close, kill on session cancel / shutdown. Watches the filesystem (via the `notify` crate) and the CLI's trace events to feed the journey sidebar \u2014 does NOT parse CLI stream events for execution (that's the wrapped-mode translator's job). The session page layout switcher (feat-054) renders the `attended` layout from the same `/session/:id` URL. Phase 11: do not start until Phase 8 wrapped mode is stable. Conformance test uses the fake CLI harness running in a long-lived mode (the harness from feat-044 is extended to support a `serve` subcommand that emits events as they occur and reads stdin for the user's keystrokes).",
      "verification": "cargo test -p weave-server -- test_terminal_trait_compiles && cargo test -p weave-server -- test_terminal_spawn_and_attach && cargo test -p weave-server -- test_terminal_detach_keeps_process && cargo test -p weave-server -- test_terminal_kill_terminates && cargo test -p weave-server -- test_terminal_filesystem_watcher_feeds_journey && cargo test -p weave-server -- test_terminal_workspaces_scoped_validation && cd web && bun run test -- --run terminal-pane && cd web && bun run test",
      "state": "not_started",
      "dependencies": [
        "feat-051"
      ],
      "evidence": ""
    },
    {
      "id": "feat-061",
      "phase": "phase-3",
      "behavior": "`docs/user/sessions.md` instructs the user to 'from the `Sessions` list, click `+ New Session` in the page header', but `web/src/app/pages/sessions.tsx` has no such button. Add a per-workspace `+ New Session` action to the Sessions list page so the doc matches the UI. Implementation: (1) Extract the inline New Session modal + form state from `web/src/app/pages/workspace.tsx` (the `<Modal>` block at the bottom plus the form state, providers fetch, and `useCreateSession` mutation) into a reusable `NewSessionModal` component in `web/src/components/new-session-modal.tsx`. The component takes `{ workspaceId, open, onClose, onCreated?(session) }` as props and owns the Provider / Specialist / Model form fields and the `useCreateSession` mutation. (2) Refactor `workspace.tsx` to use the extracted component for the existing page-level `+ New Session` button; behavior unchanged. (3) In `sessions.tsx`, render the modal once per `WorkspaceSessions` block; add a small `+ New Session` button next to the workspace heading that opens the modal pre-bound to that workspace's id; on success, navigate to `ROUTES.session(session.id)`. (4) Empty-state copy in `sessions.tsx` ('Create a workspace first to start sessions') stays as is \u2014 a workspace with no sessions should still show the per-workspace section so the button is reachable, not a global empty state. (5) The Provider dropdown is populated from `useProviders()` (already used by `workspace.tsx`); reuse the same hook in the new component. Acceptance: navigating to `/sessions` shows a visible per-workspace `+ New Session` action; clicking it opens a modal with that workspace's context; submitting creates a session and lands on `/sessions/:id`; the `workspace.tsx` `+ New Session` button still works; tsc + frontend lint clean.",
      "verification": "cd web && bun run test && cd web && bun run lint && cd web && bun run typecheck",
      "state": "passing",
      "dependencies": [
        "feat-020"
      ],
      "evidence": "./init.sh all 3 layers pass: 611 Rust tests + 88 frontend tests (5 new) all green, clippy/fmt/prettier/ESLint clean. New page test web/src/app/__tests__/sessions.test.tsx (5 cases) covers: no-workspaces empty state, per-workspace + New Session button visible when workspace has zero sessions, session rows + button coexist, click button opens the NewSessionModal, submit form calls api.sessions.create with the right payload and navigates to /sessions/:id. New component web/src/components/new-session-modal.tsx owns the form + useProviders + useSpecialists (specialist is now a dropdown, was free text) + useCreateSession mutation + inline role=alert error. workspace.tsx refactored to use the new component (page shrank 344 \u2192 220 lines). docs/user/sessions.md updated to say 'next to the workspace name' and to describe the specialist as a dropdown. Implementation note: the spec said 'render the modal once per WorkspaceSessions block' but the implementation uses one shared modal at page level (matches boards/codebases precedent, avoids N mutation instances, TanStack Query dedupes the providers/specialists queries)."
    },
    {
      "id": "feat-062",
      "phase": "phase-5",
      "behavior": "Wire the existing-but-unused `find_by_cwd_prefix` helper to sessions: a session may be **bound** to a registered codebase (git repo) at create time, and the binding becomes a runtime sandbox for the agent's FS tools. Implementation: (1) New migration `010_session_codebase_id.sql` adds `codebase_id TEXT REFERENCES codebases(id) ON DELETE SET NULL` to `sessions` plus an index. The FK is `SET NULL` so deleting a codebase unbinds the session but does not delete it. (2) `store/sessions.rs` `Session` struct gains `codebase_id: Option<String>`; all SELECT/INSERT queries, `map_row`, `create`, `create_tx` updated to round-trip the new column. (3) `api/sessions.rs` `CreateSessionRequest` gains `codebase_id: Option<String>`; the handler passes it through to the service. (4) `service/sessions.rs` `create_session` resolves `codebase_id` via `CodebaseStore::get_in_workspace` (rejects cross-workspace and unknown ids with `AppError::NotFound`); on success, the codebase's `path` is copied onto the session's `cwd` (the binding wins over any supplied `cwd`). (5) `service/sessions.rs` `run_prompt_task` sets `ToolContext.codebase_root = session.cwd` when the session is bound (and `.` when unbound); the FS tools consult this for containment. The runtime falls back to `session.cwd` as the containment root even when the bound codebase has been deleted (so the sandbox does not silently flip off). (6) `tools/fs/mod.rs` gains a new `validate_read_path` helper (sibling to `validate_write_path`) that enforces containment but skips the control-plane guard \u2014 reads of `CLAUDE.md`/`Cargo.toml` etc. are still allowed. `fs_read`, `fs_list`, and `fs_search` (explicit-path branch) all call it. (7) `tools/shell.rs` and `tools/git/mod.rs` `validate_git_cwd` call `validate_read_path` on the explicit-`cwd` form so `shell_exec { cwd: '/etc' }` and `git_log { cwd: '/etc' }` are rejected for bound sessions. (8) `tools/fs/list.rs` and `tools/fs/search.rs` walkers use `entry.file_type()` (not `path.is_dir()`) and skip symlinks entirely \u2014 this is the load-bearing piece of the sandbox: `ln -s /etc <repo>/etc_link` cannot be used to escape via `fs_list`/`fs_search`. (9) `store/codebases.rs` drops the `#[allow(dead_code)]` on `find_by_cwd_prefix` since it now has a production caller; the `delete` method's doc comment is updated to reflect the new FK behavior. (10) Frontend: `Session` interface gains `codebase_id: string | null`; `NewSessionModal` adds a `Codebase` dropdown populated by `useCodebases(workspaceId)` with a disabled empty-state + link to `/codebases` when no codebases are registered; on submit, `codebase_id` is sent in the create payload. (11) `app/pages/session.tsx` session header shows a monospace pill with the codebase basename (full path on hover) when `session.cwd` is set. (12) Docs: `docs/user/sessions.md` describes the new `Codebase` field and adds a 'How sessions use a codebase' section that explicitly states the sandbox applies to the cwd arg / fs_read / fs_list / fs_search / explicit-cwd form of shell+git, NOT to the shell command body itself (so a bound session is a permission boundary on cwd, not a jail on the shell). `docs/user/codebases.md` mirrors the same section and updates the 'I registered a codebase but my session does not see it' pitfall. (13) Out of scope: kanban auto-spawn still passes `codebase_id: None` (the `tasks` model has no codebase ref); resume does not auto-inherit parent's binding. Acceptance: `cargo test -p weave-server` and `cd web && bun run test` green, `./init.sh` runs the system smoke (`/api/health` + `curl / | grep 'id=\"root\"'`); user can open `+ New Session`, pick a codebase, submit; the session page header shows the path pill; bound sessions enforce FS containment at the cwd-arg level and refuse to follow symlinks inside the repo.",
      "verification": "cargo test -p weave-server && cd web && bun run test && ./init.sh",
      "state": "passing",
      "dependencies": [
        "feat-032",
        "feat-061"
      ],
      "evidence": "Layers 1 (lint) + 2 (tests) pass via `just check`: 616 Rust tests (5 new) + 90 frontend tests (2 new) green, clippy/fmt/prettier/ESLint clean. Layer 3 (system smoke + agent-browser validation) is the user's manual step \u2014 the verification command in the spec runs `./init.sh` and the user has signaled they'll drive the agent-browser smoke themselves; PROGRESS.md 'feat-062' section tracks the manual-validation checklist. New store tests: test_session_with_codebase_id (round-trips the new column), test_session_codebase_delete_sets_null (ON DELETE SET NULL keeps the row, drops the binding). New service tests: test_create_session_with_codebase_id_copies_path_to_cwd (binding wins over any supplied cwd), test_create_session_invalid_codebase_id_rejected (unknown id \u2192 AppError::NotFound), test_create_session_cross_workspace_codebase_rejected (ws-A codebase rejected for ws-B session). The legacy test suite (200+ fs/git/shell tests) stays green because containment is opt-in (only bound sessions are contained). New frontend tests in web/src/app/__tests__/sessions.test.tsx: empty-codebases disabled dropdown + /codebases link, and submit-with-codebase sends codebase_id in the create payload. Migration 010 is additive (1 column + 1 index); existing rows keep codebase_id = NULL. docs/user/sessions.md has a 'How sessions use a codebase' section that explicitly states the dual claim (cwd-arg containment yes, shell-body jail no); docs/user/codebases.md mirrors it. The simplify review pass: the runtime `codebase_root` resolution in run_prompt_task collapsed from 47 lines (with a SQL `find_by_cwd_prefix` call) to 3 lines (cwd when bound, . when unbound), because the runtime was recovering information already on the session row. The security-review pass: fixed a real symlink-escape bug in the fs_list/fs_search walkers (was using `path.is_dir()` which follows symlinks, now uses `entry.file_type()` and skips symlinks entirely); rewrote the user docs to make the dual claim explicit (the sandbox is on the cwd arg, not the shell command body)."
    },
    {
      "id": "feat-063",
      "phase": "phase-12",
      "behavior": "Rich kanban auto-spawn prompt: replace the 3-line `build_initial_prompt` (crates/weave-server/src/service/kanban.rs:24, returns `\"Process task: {title}\\n{description}\"`) with a structured 10-section prompt builder so the agent knows which column it's in, what gates block the next move, what artifacts are required, and what the previous lane did. Implementation: (1) New file `crates/weave-server/src/service/kanban_prompt.rs` defines `pub struct KanbanPromptContext<'a> { task: &'a Task, column: &'a Column, board: &'a Board, lane_sessions: Vec<LaneSession>, present_artifact_types: HashSet<String>, is_first_lane_run: bool }` and a free `pub fn build_kanban_prompt(ctx: &KanbanPromptContext) -> String`. (2) The 10 sections, in order, are: header line ('You are assigned to Kanban task: {title}'); Context ('You are working in Kanban context. Use tools to manage this card.'); Task Details (Card ID, Board ID, Current Column, Next Column, Priority, Labels, GitHub Issue); Objective (task.objective or task.description); Story Readiness (only when column.stage is 'backlog', derived from task.description's existing acceptance_criteria / verification_commands / test_cases \u2014 flags what's present, what's missing); Artifact Gates (current lane's required artifacts + next transition's required artifacts, plus a pointer to list_artifacts / provide_artifact \u2014 N/A when no artifacts required); Delivery Gates (only when the next column has delivery rules \u2014 committed changes, clean worktree, PR-ready branch); Contract Gates (only when the next column requires the canonical story \u2014 description must contain a parseable `yaml` code block); Lane History (previous runs in this lane: provider, role, start/end time, status, summary); Lane Handoff Context (the previous lane's session descriptor and any pending handoffs); Available Tools (column-stage-specific tool list with card-specific IDs); Instructions (column-stage-specific step-by-step guidance, 8-12 steps). (3) The gathering helpers (`fetch_lane_sessions`, `load_present_artifact_types`) live in the same module and pre-load what they need from `TaskStore`, `BoardStore`, `SessionStore`, and `ArtifactStore` so the builder itself stays a pure function over its inputs. (4) `service/kanban.rs::try_automate_lane` builds the `KanbanPromptContext` before calling `SessionService::send_prompt` and passes the rendered string; the old `build_initial_prompt(task: &Task)` function is kept as a thin shim that calls `build_kanban_prompt(&ctx_from_task_only)` so the A2A path (which has no column / board / lane history) keeps working with no behavior change. (5) Specialist `.md` files in `crates/weave-server/resources/specialists/` get a one-line note in their system prompt reminding the model to read the kanban context sections in order (the per-section content itself is dynamic and lives in `kanban_prompt.rs`). Acceptance: `cargo test -p weave-server -- build_kanban_prompt` covers each of the 10 sections in isolation (helper functions return stable strings) and one end-to-end test that calls the old `build_initial_prompt` shim and asserts the new builder is wired in. Existing `test_kanban_autospawn_*` tests stay green (the A2A path uses the shim; the kanban path gets the rich prompt). Line budget: ~700 lines (300 impl + 250 tests + 50 mod wiring + 100 specialist doc updates).",
      "verification": "cargo test -p weave-server -- build_kanban_prompt && cargo test -p weave-server -- test_kanban_autospawn && ./init.sh",
      "state": "passing",
      "dependencies": [
        "feat-051",
        "feat-055"
      ],
      "evidence": "Three-layer gate green on commit 957d509. Layer 1: cargo clippy + cargo fmt clean. Layer 2: cargo test -p weave-server -- build_kanban_prompt → 2 passed (test_build_kanban_prompt_sections_in_order_for_backlog, test_build_kanban_prompt_omits_backlog_sections_for_non_backlog); cargo test -p weave-server -- test_kanban_autospawn → 4 passed (test_kanban_autospawn_uses_column_runtime_kind + 3 others); full Rust suite 844 passed (was 828, +16 net from 30 new pure-formatter tests + 6 new ctx tests + 5 new code-review tests - 18 deleted/exchanged). Layer 3: ./init.sh green — server starts, /api/health 200, GET / serves index.html, real claude --version health-check succeeds, clean shutdown. Two new files: crates/weave-server/src/service/kanban_prompt.rs (pure 12-slot builder + 29 sync tests, no DB) and crates/weave-server/src/service/kanban_prompt_ctx.rs (async store-IO assembler + 6 tokio integration tests, uses make_test_state). Modified: service/kanban.rs (deleted 11-line build_initial_prompt shim + 2 dead tests; replaced call site at 235 with assemble_kanban_prompt_context().await? + build_kanban_prompt(ctx)); service/mod.rs (2 pub mod lines); api/kanban.rs (the end-to-end test_kanban_autospawn message-content assertion updated from the old 3-line string to assert the new builder's 5 structural substrings + a !content.contains('Process task:') regression guard). 5 specialists (backlog-refiner, dev-crafter, todo-orchestrator, review-guard, done-reporter) each gained a 4-line '## Kanban Context (feat-063)' section. A2A path verified untouched: a2a/messages.rs has zero references to build_initial_prompt / build_kanban_prompt. Phase 6 code-review pass applied 5 high-confidence fixes: description truncated at 8 KB with marker (chars, not bytes — no UTF-8 mid-codepoint splits), is_first_lane_run renamed to is_first_active_run + Section 4 wording changed to 'first card in this lane to start a session', SessionStore::get_by_id workspace-scope defensive check + tracing::warn! in the assembler, Markdown table `|` escape via escape_table_cell, leading blank line in prompt removed. 2 perf items deferred to out-of-scope (N+1 session lookups in lane-history loop; TaskStore::list_by_board loads entire board). DECISIONS.md entry: 2026-06-12: feat-063 — KanbanPromptContext owns its Task/Column/Board, the build_initial_prompt A2A shim is deleted."
    },
    {
      "id": "feat-064",
      "phase": "phase-12",
      "behavior": "Six kanban tools for agents: `move_card` (already exists at crates/weave-server/src/tools/kanban/move_card.rs:33 and is left untouched), plus 5 new tools that let the agent talk back to the board \u2014 `update_card`, `update_task`, `list_artifacts`, `provide_artifact`, `create_note`. **Sub-split warning:** this task is the largest of the phase (~1500 lines estimated: 5 tools \u00d7 ~250 lines impl + 5 \u00d7 ~150 tests + ~100 lines of mod wiring). If it exceeds the 1000-line PR budget at implementation time, split into feat-064a (update_card + update_task + create_note, the planning-side tools that mutate the card; ~750 lines) and feat-064b (list_artifacts + provide_artifact, the evidence-side tools that read/write artifacts; ~500 lines). Implementation outline for the full 6-tool set: (1) New files `crates/weave-server/src/tools/kanban/{update_card,update_task,list_artifacts,provide_artifact,create_note}.rs`, each defining a struct that implements `ToolExecutor` (name, description, input_schema, execute). All five delegate to existing store methods \u2014 `update_card` \u2192 `TaskStore::update` (title/description/priority/labels), `update_task` \u2192 `TaskStore::update` (scope/acceptance_criteria/verification_commands/test_cases), `list_artifacts` \u2192 `ArtifactStore::list_for_task` (read-only), `provide_artifact` \u2192 `ArtifactStore::create` (with mime-type detection), `create_note` \u2192 `NoteStore::create` (workspace-scoped). (2) `crates/weave-server/src/tools/kanban/mod.rs` adds `pub mod` + `pub use` for each; the existing `test_kanban_tools` integration test in the `tests` submodule is extended to call all 6 tools in sequence. (3) `crates/weave-server/src/tools/mod.rs` registers the 5 new tools in `ToolRegistry`; profile bindings per design doc \u00a75.2: `full` and `implementation` profiles get all 5; `planning` gets `update_card` + `update_task` + `create_note` but NOT `list_artifacts` / `provide_artifact` (the planning profile is read-only on evidence). (4) Reuse the `require_string` / `optional_string` / `check_optional_status` / `error` / `success` helpers from `tools/fs/mod.rs:1` (already used by `move_card.rs:30`) for input validation \u2014 do NOT write new validation glue. (5) The conformance test in `tests/cli_conformance.rs` gains two new cases: a fake CLI emits `tool_use` for `update_card` (assert the task row in DB has the new title) and for `provide_artifact` (assert a new row appears in `artifacts`). Acceptance: all 6 tools appear in `ToolRegistry::all_definitions`; profiles resolve correctly; `cargo test -p weave-server -- test_kanban_tools` covers all 6; conformance test green; `./init.sh` 3-layer gate green.",
      "verification": "cargo test -p weave-server -- test_kanban_tools && cargo test -p weave-server -- test_tool_registry && cargo test -p weave-server --test cli_conformance && ./init.sh",
      "state": "not_started",
      "dependencies": [
        "feat-031",
        "feat-051",
        "feat-063"
      ],
      "evidence": ""
    },
    {
      "id": "feat-065",
      "phase": "phase-12",
      "behavior": "Column-stage awareness: each column declares a `stage` enum (Backlog | Todo | Dev | Review | Done) that controls prompt variation, available tools, and allowed target-column transitions. Implementation: (1) New migration `crates/weave-server/src/migrations/014_column_stage.sql`: `ALTER TABLE columns ADD COLUMN stage TEXT NOT NULL DEFAULT 'dev'` plus heuristic backfill \u2014 `Backlog \u2192 backlog`, `To Do \u2192 todo`, `In Progress \u2192 dev`, `Review \u2192 review`, `Done \u2192 done`. New columns default to `'dev'` (the most permissive stage) so existing boards are unaffected. (2) `crates/weave-server/src/store/columns.rs::Column` gains `pub stage: ColumnStage`; a new `pub enum ColumnStage { Backlog, Todo, Dev, Review, Done }` with `as_str` / `FromStr` round-trip and a `default()` returning `Dev`. `ColumnStore::create_tx`, `map_row`, and `update` round-trip the new field; the existing `default_column` test helper gets `stage: ColumnStage::default()`. (3) `kanban_prompt.rs::build_kanban_prompt` (from feat-063) reads `column.stage` and varies the `Instructions` section (8-12 stage-specific steps) and the `Available Tools` list (e.g. backlog stage hides `move_card` for non-backlog targets; done stage is read-only). (4) `crates/weave-server/src/tools/kanban/move_card.rs` rejects moves that skip more than one stage \u2014 `backlog \u2192 dev` is allowed (the common 'refine then start' flow), `backlog \u2192 done` is rejected with a clear `AppError::validation` error naming the stage gap. The same-column no-op path is unchanged. (5) `crates/weave-server/src/api/columns.rs` POST and PATCH handlers accept an optional `stage` field (string from the wire; `FromStr` parses; default `dev` when missing). (6) `crates/weave-server/src/store/kanban_test_helpers.rs::default_column` and `seed_workspace_with_board` get `stage: ColumnStage::Dev` to keep existing tests green. Acceptance: `cargo test -p weave-server -- test_columns_migration_adds_stage` mirrors the existing `test_columns_migration_adds_runtime_kind` shape; `test_build_kanban_prompt_stage_backlog` asserts the backlog-stage prompt hides the implementation steps; `test_move_card_stage_filter` asserts `backlog \u2192 done` returns the stage-gap error. ./init.sh stays green.",
      "verification": "cargo test -p weave-server -- test_columns_migration_adds_stage && cargo test -p weave-server -- test_build_kanban_prompt_stage_backlog && cargo test -p weave-server -- test_move_card_stage_filter && ./init.sh",
      "state": "not_started",
      "dependencies": [
        "feat-024",
        "feat-063"
      ],
      "evidence": ""
    },
    {
      "id": "feat-066",
      "phase": "phase-12",
      "behavior": "Full transition-gate enforcement: extend `service::kanban::check_transition_gates` (crates/weave-server/src/service/kanban.rs:312, currently evaluates 3 gates: freeze_description, required_fields, required_artifact_types) to also evaluate 4 new gate types \u2014 delivery, contract, checklist, validator \u2014 and add a `gate_mode` (blocking | warning) toggle. Implementation: (1) New migration `crates/weave-server/src/migrations/015_column_automation.sql`: `ALTER TABLE columns ADD COLUMN automation_json TEXT` (nullable; NULL = no automation config, the pre-existing behavior). (2) `crates/weave-server/src/store/columns.rs::Column` gains `pub automation: Option<AutomationConfig>`; a new `pub struct AutomationConfig { required_artifacts: Vec<String>, delivery_rules: DeliveryRules, contract_rules: ContractRules, checklist_rules: ChecklistRules, validator_command: Option<String>, gate_mode: GateMode }` with the JSON shape from design doc \u00a75.4. `ColumnStore::create_tx`, `map_row`, and `update` round-trip the field; existing rows keep `automation = None`. (3) `check_transition_gates` gains 4 new branches: delivery (for each rule in `dest.automation.delivery_rules`, check `require_committed_changes` via `git status --porcelain` on the session's cwd and `require_clean_worktree` via `git diff --quiet`); contract (when `require_canonical_story`, parse a `\\`\\`\\`yaml` code block from `task.description` and assert the required fields are present); checklist (when `required_checklist` is set, scan `task.evidence` for matched `[x]` items \u2014 the existing `task.verification_report` text is the natural evidence surface); validator (when `validator_command` is set, run it as a subprocess with a 30s timeout and require `status == 0`; the result is cached on a `kanban_validations` table to avoid re-runs). (4) `gate_mode: blocking` (default) rejects the move on gate failure with a structured `AppError::validation_with_code`; `gate_mode: warning` allows the move but logs a `tracing::warn!` with the gate details. (5) `crates/weave-server/src/api/columns.rs` POST/PATCH handlers accept the `automation` JSON; the move_card tool endpoint returns `{ error: { code, message, gate_type, missing } }` when a gate fails (caller can branch on `gate_type`). (6) Reuse the existing `AppError::validation_with_code` pattern at `service/kanban.rs:192` (the `cwd_outside_codebase` case is the template) for the structured error response. **Budget note:** the validator_command gate needs a subprocess runner (~150 lines for runner + tests) and may push this task over the 1000-line PR budget. If so, defer `validator_command` to a follow-up feature (or split feat-066 into 066a: schema + artifact/delivery/contract gates, and 066b: checklist + validator + gate_mode). Acceptance: `cargo test -p weave-server -- test_check_transition_gates_artifact_gate` (extends the existing 3-gate tests), `test_check_transition_gates_delivery_gate` (uses a `tempfile`-based fake git repo with committed + clean worktree states), `test_check_transition_gates_contract_gate` (parses a sample `\\`\\`\\`yaml` block), `test_check_transition_gates_gate_mode_warning` (warning mode lets the move through and emits a `tracing::warn!`), `test_move_card_gate_failed_error_shape` (asserts the error JSON contains `gate_type` + `missing`). Existing `test_move_card_transition_gates` and `test_check_transition_gates_*` tests stay green (the new branches are additive, gated on `dest.automation.is_some()`).",
      "verification": "cargo test -p weave-server -- test_check_transition_gates && cargo test -p weave-server -- test_move_card_gate_failed_error_shape && ./init.sh",
      "state": "not_started",
      "dependencies": [
        "feat-028",
        "feat-031",
        "feat-065"
      ],
      "evidence": ""
    },
    {
      "id": "feat-067",
      "phase": "phase-12",
      "behavior": "Lifecycle supervision for kanban-auto-spawned Claude Code sessions: a tokio watchdog that detects stalled sessions, re-prompts them with a 'you appear stalled, continue' prefix, stops recovery after `max_recovery_retries` (default 2), and broadcasts a `SessionFailed` SSE event on the board channel when the limit is hit. Implementation: (1) New migration `crates/weave-server/src/migrations/016_kanban_session_watch.sql`: a `kanban_session_watch(session_id PRIMARY KEY REFERENCES sessions(id) ON DELETE CASCADE, task_id TEXT NOT NULL, last_activity_at TEXT NOT NULL, recovery_count INTEGER NOT NULL DEFAULT 0, status TEXT NOT NULL DEFAULT 'watching')` table. The `last_activity_at` is bumped on every SSE event for the session (the `SseManager::broadcast` path is the natural hook) and on every `send_prompt` call. (2) New file `crates/weave-server/src/service/kanban_lifecycle.rs` defines `pub struct KanbanLifecycleSupervisor` with `start(state: AppState, shutdown: CancellationToken) -> JoinHandle<()>` (spawns a `tokio::time::interval` task at 30s, mirrors the orphan-reaping pattern at `service/startup.rs:1`) and `scan_once(&state, now: Instant) -> ScanReport` (pure function, returns the list of stalled sessions, for testability). A session is 'stalled' when `now - last_activity_at > stall_threshold_seconds` (default 300s, per-column override via `column.automation.stall_threshold_seconds` once feat-066 lands). (3) `service/kanban.rs::try_automate_lane` writes the initial `kanban_session_watch` row after creating the session (lines 207-229). (4) On stall detection, the supervisor calls `SessionService::send_prompt(session_id, &format!(\"You appear stalled (no activity in {seconds}s). Continue the kanban task. If you're done, call `move_card` to advance the card; if you're blocked, call `update_card` with a comment explaining the blocker.\\n\\n{last_assistant_message_excerpt}\"))` and increments `recovery_count`. (5) When `recovery_count > max_recovery_retries`, the supervisor sets the row's `status = 'failed'`, broadcasts `SseWireEvent::SessionFailed { session_id, task_id, reason: 'max_recovery_retries_exceeded' }` on the `board:{board_id}` channel (same pattern as `SessionStarted` at `kanban.rs:249-257`), and stops watching. (6) `crates/weave-server/src/sse/mod.rs` gains the `SessionFailed` variant. (7) `lib.rs::run_cleanup` (or a new helper) stops the supervisor on shutdown within the `CLEANUP_BUDGET` (5s, per `lib.rs:19`). Acceptance: `cargo test -p weave-server -- test_kanban_lifecycle_stall_detected` (fake clock advances past the threshold; supervisor reports 1 stalled session), `test_kanban_lifecycle_recovery_resends_prompt` (asserts `send_prompt` is called with the stalled prefix), `test_kanban_lifecycle_max_retries_emits_failed_event` (asserts `SseWireEvent::SessionFailed` after 2 recovery attempts), `test_kanban_lifecycle_shuts_down_cleanly` (asserts supervisor stops within `CLEANUP_BUDGET`). The conformance test in `tests/cli_conformance.rs` gains a case: a fake CLI emits no events for 6 minutes, the watchdog fires, and the recovery prefix reaches the CLI's stdin. `./init.sh` 3-layer gate green. Line budget: ~800 lines (50 migration + 300 supervisor impl + 50 watch table hookup + 200 lifecycle tests + 100 SSE variant + 100 main.rs spawn).",
      "verification": "cargo test -p weave-server -- test_kanban_lifecycle && cargo test -p weave-server --test cli_conformance && ./init.sh",
      "state": "not_started",
      "dependencies": [
        "feat-051",
        "feat-064",
        "feat-066"
      ],
      "evidence": ""
    }
  ]
}